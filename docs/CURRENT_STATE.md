# ShelfCash Backend Current State

## Baseline

- **Date:** 2026-09-07 (Asia/Saigon)
- **Branch:** `main`
- **HEAD:** `1d7d0c0` (`docs: establish backend pre-refactor baseline`), tagged `pre-refactor-backend-2026-09`.
- **Origin:** `main` is 0 ahead / 0 behind `origin/main` at the baseline freeze.
- **Working tree:** clean before DOC-CLEANUP-0.
- **Runtime:** Python 3.13.13; project requirement Python >=3.11; FastAPI 0.140.7; SQLAlchemy 2.0.51; Pydantic 2.13.4.
- **Default persistence:** SQLAlchemy with SQLite at `sqlite:///runtime/shelfcash.db`; Alembic head observed through migration `20260827_0025` in tests.

## Current runtime architecture

ShelfCash Backend is a FastAPI modular monolith. HTTP routes call application services, repositories/direct SQLAlchemy access, and deterministic computation adapters. `shelfcash_core` is the production computation authority for Forecast, BOM, Ingredient Demand and Decision/What-if computation. `shelfcash_forecast` is the optional forecast/BOM shadow-parity implementation; Decision Intelligence imports some non-computation contracts/helpers from it, which does not confer production authority. Under ADR-014, the operational `/procurement-plans` planner is a distinct accepted authority rather than a shadow of the Decision core. Persistence uses SQLAlchemy and SQLite by default. OpenRouter/Qwen is a bounded narrative or ambiguity-resolution integration; it is not business authority.

## Post-R7 stochastic risk-evidence sufficiency slice

- **Status:** COMPLETE (2026-09-19). ADR-016 makes empirical stochastic stockout risk authoritative only after scenario evidence passes `MIN_EFFECTIVE_STOCHASTIC_SCENARIOS = 10`.
- **Runtime behavior:** Decision Procurement canonicalizes generated ingredient-demand paths by exact business demand vector, aggregates duplicate-path weights, and calculates `N_eff = 1 / sum(w_i ** 2)` on normalized unique-path weights. When the evidence is insufficient, it uses the existing p25/p50/p75 deterministic design scenarios, sets `stochastic_saa_enabled=false`, and records `stochastic_fallback_reason=insufficient_effective_scenarios`.
- **Authority boundary:** insufficiency is not high risk. `RISK_CONSTRAINT_VIOLATION` is not emitted from an insufficient stochastic set. Exact FEFO and the critic still evaluate the deterministic fallback, so exact safety-floor and all other existing feasibility failures remain authoritative. Stochastic thresholds, optimizer objective, candidate-selection rule, persistence shape, and historical Decision Runs are unchanged.
- **Diagnostics / compatibility:** current packages add optional `scenario_count_requested`, `scenario_count_generated`, `unique_scenario_count`, `effective_scenario_count`, and `stochastic_fallback_reason` technical metrics. Old packages without these fields remain readable; no migration or backfill is required.
- **Verification:** focused scenario/procurement/FEFO/strategy/Decision/What-if regression passed **156 tests, 15 warnings in 88.24s**; full suite passed **656 tests, 22 warnings in 553.07s**. `compileall -q app shelfcash_core tests` and `git diff --check` passed. Generated OpenAPI remains **58 paths / 70 operations** and contains the additive technical-metric fields. No endpoint or route is added.

## Core business flow

Import → Canonical DB → Forecast → BOM → Ingredient Demand → Inventory/FEFO → Procurement candidates → Exact simulation/critic → Strategy selection → Decision Package → Decision Intelligence.

## Narrative / LLM status

- **Strategy Explanation:** deterministic DEFAULT; optional Qwen `llm_polish`; default `GET /brief` does not call Strategy Qwen.
- **Ingredient Synthesis:** deterministic DEFAULT for NORMAL/WATCH/CRITICAL; optional Qwen polish where configured; exact CRITICAL evidence authority remains enforced and missing exact evidence fails closed as `LIMITED_EVIDENCE`.
- **Overall Summary:** HYBRID; authorized facts → Qwen primary wording → strict backend validation → deterministic safe fallback. Deterministic-first migration is not accepted.
- **Decision Explanation:** grounded/guarded LLM where available, with deterministic/template fallback.
- **What-if:** deterministic business recomputation with grounded/hybrid explanation.
- **Excel Mapping:** deterministic/rule-first; LLM is reserved for genuine semantic ambiguity.

## API baseline

- Runtime OpenAPI: **58 paths / 70 operations**, matching `CURRENT_API_CONTRACT.md` by count.
- Restored route manifest: **58 unique paths / 70 operations**; exact manifest tests pass.
- Most `/api/v1` business routes use the `x-shelfcash-key` header; health and framework documentation endpoints are public. OpenAPI exposes the API key as a header parameter rather than a security scheme.
- Runtime errors use `{code, message, details, request_id}`. R1.1 aligns generated validation `422` and representative common-error OpenAPI responses to this runtime envelope.
- Typed boundaries include import, ingredient catalog, recipes, Menu/Product, operational page reads, Completion writes, Purchase Orders, forecast, IngredientDemandRun, ProcurementPlanRun, raw Decision Package, legacy forecast/plan metadata/results, Decision Brief, Explanation, What-if, and inventory-constraint writes. Lower-risk untyped read boundaries remain outside the approved R1 slices.

## Known technical debt

- `shelfcash_core` / `shelfcash_forecast` retain overlapping/diverged computation. R3 resolved authority ambiguity but did not remove the maintenance burden: forecast/BOM shadows are intentional parity implementations; FEFO and strategy/risk are semantic alternates; package consolidation remains deferred.
- Decision Package now has a typed current public boundary. Its intentionally broad nested metrics/diagnostics remain bounded JSON objects; old persisted JSON compatibility is not required for disposable SQLite state.
- Untyped bootstrap, dashboard, settings-read, import-result, and other lower-risk read responses remain outside R1's approved vertical slices.
- `DecisionPlanningService` spans demand, procurement, Decision Run generation/read, brief, summary/synthesis persistence, explanation, What-if, and legacy compatibility.
- Mixed repository and direct ORM usage; transaction ownership is not uniform.
- Canonical and legacy/compatibility APIs overlap without one explicit lifecycle registry.
- Persisted JSON blobs retain some permissive reads in Decision Intelligence. Under ADR-013, old SQLite rows do not require migration/backfill compatibility; preserve only behavior that supports the current public runtime contract.
- OpenRouter transport/guard complexity remains broad relative to the reduced default LLM scope.
- Dense responsibility hotspots are confirmed in planning/completion route and service modules.

## R0 verification

- All 19 pre-existing tracked deletions were restored exactly from HEAD.
- Full suite: **577 passed, 0 failed, 0 skipped, 22 warnings in 142.39 seconds**.
- Route-manifest and frontend-document dependency tests pass.
- OpenAPI generation remains **58 paths / 70 operations**.
- Python compile and Git whitespace gates pass.
- Initial migration, import-fixture, and logger-capture failures were classified and repaired without changing runtime business behavior.
- The reviewed baseline is committed and tagged `pre-refactor-backend-2026-09`.

## R1.1 verification

- **Status:** COMPLETE — Common error/OpenAPI contract alignment.
- Runtime status codes, envelope code/details semantics, request-ID generation/header behavior, success payloads, routes, business behavior, and persistence behavior are unchanged.
- Generated OpenAPI now publishes `ErrorResponse` for all 68 automatic validation `422` responses, authenticated-operation `401` responses, and representative runtime-evidenced `404` (`GET /imports/{import_id}`), `409` (`POST /stores/{store_id}/ingredients`), and `500` (`POST /forecast-models/train`) responses. FastAPI `HTTPValidationError` is no longer published.
- New contract coverage: `tests/test_error_openapi_contract.py` freezes validation, auth, not-found, conflict, server-error, success-schema, and 58-path/70-operation behavior.
- Verification: targeted error/OpenAPI/route/forecast tests: **18 passed**; full suite: **581 passed, 0 failed, 0 skipped, 22 warnings in 205.82 seconds**; `compileall -q app shelfcash_core tests` and `git diff --check` passed.
- Rollback is limited to the centralized OpenAPI builder, `ErrorResponse` schema requirement alignment, its composition-root registration, the R1.1 tests, and this checkpoint documentation. No migration, data backfill, business rollback, or Decision Run migration is required.

## R1.2 verification

- **Status:** COMPLETE — Decision Package Typed Boundary.
- `POST /api/v1/stores/{store_id}/decision-runs` and `GET /api/v1/decision-runs/{decision_run_id}` now reference explicit `DecisionPackage` OpenAPI/Pydantic responses. Current wire payload, dates, numbers, statuses, routes, business behavior, and narrative routing are unchanged.
- The current package has typed top-level identity/status/strategy/plan/ingredient-demand/inventory/strategy-selection/critic/reason/warning/technical boundaries. Deliberately broad business metrics and diagnostic JSON remain bounded nested objects; `assistant` and `recommended_strategy` remain optional/nullable.
- ADR-013 establishes that Kaggle SQLite state is disposable. Old persisted Decision Run or Decision Package JSON compatibility, migration, and backfill are not required; public HTTP/client compatibility remains mandatory under ADR-007.
- Five historical-fixture assertions were redirected from raw GET to direct fixture-state checks because their partial old package shapes are not the current typed raw HTTP contract. `/brief` fallback behavior remains covered.
- Verification: targeted Decision Package/Decision Assistant/OpenAPI/route tests: **128 passed**; full suite: **581 passed, 0 failed, 0 skipped, 22 warnings in 158.99 seconds**.

## R1.3 verification

- **Status:** COMPLETE — IngredientDemandRun Typed Response.
- `POST` and `GET /api/v1/stores/{store_id}/forecast-runs/{forecast_run_id}/ingredient-demand` now reference the explicit `IngredientDemandRunResponse` schema. Current generated payload fields, prediction ordering, IDs, statuses, warnings, failure fields, ISO date/datetime serialization, and numeric serialization are unchanged.
- Typed boundaries cover the run, prediction, and stable BOM contribution evidence. Prediction p25/p50/p75 remain JSON numbers; established contribution `product_*` and `ingredient_*` evidence quantiles remain strings.
- `blocked` runs remain readable through GET with nullable failure fields and an empty prediction list where persisted. Empty scoped-demand warnings remain valid responses. No service, BOM, persistence, procurement, Decision Run, or narrative behavior changed.
- OpenAPI verification: both operations reference `#/components/schemas/IngredientDemandRunResponse`; runtime surface remains **58 paths / 70 operations**. Targeted demand/BOM/procurement/Decision/OpenAPI/route tests: **69 passed**. Full suite: **582 passed, 0 failed, 22 warnings in 150.48 seconds**.
- Rollback is limited to the IngredientDemand response schemas, the two route declarations, R1.3 contract tests, and these checkpoint/plan notes. No Alembic migration, data backfill, or SQLite data rollback is required.

## R1.4 verification

- **Status:** COMPLETE — ProcurementPlanRun Typed Response.
- `POST` and `GET /api/v1/stores/{store_id}/forecast-runs/{forecast_run_id}/procurement-plans` now reference `ProcurementPlanRunResponse`. The current run/plan/line fields, strategy names, recommendation semantics, sorted plan/line ordering, timestamps, dates, warnings, and failure fields are unchanged.
- Typed boundaries cover run, plan, plan line, inventory daily projection, daily row, and consumed lot. Plan quantity/risk fields and line quantities remain JSON floats where the current serializer emits floats; daily simulation quantities intentionally remain strings; dynamic `metrics` remains a bounded JSON object.
- Completed no-feasible runs remain valid with nullable `recommended_strategy`; persisted blocked runs remain readable with nullable failure fields and no plans. No optimizer, scenario, exact resimulation, supplier/order, persistence, Decision Run, legacy Plan API, or narrative behavior changed.
- OpenAPI verification: both operations reference `#/components/schemas/ProcurementPlanRunResponse`; runtime surface remains **58 paths / 70 operations**. Targeted procurement/strategy/Decision/OpenAPI/route tests: **90 passed**. Full suite: **584 passed, 0 failed, 22 warnings in 152.04 seconds**.
- Rollback is limited to Procurement Plan response schemas, the two route declarations, R1.4 contract tests, and these checkpoint/plan notes. No Alembic migration, data backfill, or SQLite data rollback is required.

## R1.5 verification

- **Status:** COMPLETE — Menu/Product Response DTO.
- `GET`/`POST /api/v1/stores/{store_id}/products`, `PATCH /api/v1/stores/{store_id}/products/{product_id}`, `PUT /api/v1/stores/{store_id}/products/{product_id}/components`, and `GET /api/v1/stores/{store_id}/menu` now have explicit `MenuProductResponse` or `MenuResponse` OpenAPI/Pydantic boundaries.
- Product, component, menu-summary, and menu-read DTOs preserve the current product fields, nullable SKU/price/list-price/selling-unit fields, price numeric types, duplicate component identity aliases, status/active/item-type/version meanings, filtering, paging, and deterministic ordering. Fresh datetime values and existing idempotency-replay timestamp strings are both preserved rather than normalized.
- OpenAPI verification: list products publishes an array whose items reference `#/components/schemas/MenuProductResponse`; create/patch/components publish that DTO directly; menu publishes `#/components/schemas/MenuResponse`. Runtime surface remains **58 paths / 70 operations** with no unrelated response-schema changes.
- No catalog/menu service, import, recipe, persistence, combo, pricing, or optimistic-concurrency behavior changed. No old SQLite compatibility code was removed and no migration/backfill is required.
- Verification: targeted Product/Menu/recipe/import/OpenAPI/route tests: **70 passed**; full suite: **588 passed, 0 failed, 22 warnings in 219.86 seconds**; `compileall -q app shelfcash_core tests` and `git diff --check` passed.
- Rollback is limited to the Menu/Product response schemas, five response-model declarations, `tests/test_menu_response_contract.py`, and these checkpoint/plan notes. No data rollback is required.

## R1.6 verification

- **Status:** COMPLETE — Operational `Page<T>` Response DTOs.
- `GET /api/v1/stores/{store_id}/imports`, recipe-version history, sales history, usage history, purchase history, inventory, inventory movements, and calendar features now reference explicit `Page<T>` response schemas. The reusable `Page[T]` is a response-only DTO utility; it does not change paging/query infrastructure.
- Current item shapes are frozen exactly: Import summaries and recipe-version summaries have dedicated DTOs; sales/usage/purchase history and inventory movements retain their current public ORM-derived fields (including public movement `source_profile_id`); inventory retains its computed FEFO/expiry view; calendar retains its reduced read view. Public Decimal-derived quantities remain JSON numbers, recipe-line quantities remain strings, and date/datetime serialization remains unchanged.
- Paging remains `{items, page, page_size, total}`, with `page=1`, `page_size=50`, `page >= 1`, and `1 <= page_size <= 200`. Existing filters, deterministic ordering, beyond-result empty pages, empty results, statuses, auth/error behavior, and persistence are unchanged. At R1.6 completion, Purchase Order list and Completion writes were deliberately deferred to R1.7; R1.7 has since typed those boundaries.
- OpenAPI verification: all eight scoped `200` responses now reference their concrete generated `Page_<ItemResponse>_` schemas; paths remain **58**, operations remain **70**. R1.1–R1.5 error, DecisionPackage, ingredient-demand, procurement-plan, and Menu boundaries remain unchanged.
- Verification: `tests/test_operational_page_contract.py` **3 passed**; full suite: **591 passed, 0 failed, 22 warnings in 512.13 seconds**. No Alembic migration, data backfill, or SQLite compatibility cleanup was needed.
- Rollback is limited to `app/schemas/operational.py`, the eight response-model declarations in `app/api/operational.py`, `tests/test_operational_page_contract.py`, and this checkpoint/plan documentation. Because the worktree contains R1.1–R1.5 WIP, documentation rollback must be hunk-selective. No data rollback is required.

## R1.7 verification

- **Status:** COMPLETE — Completion / Purchase Order Response DTOs.
- Explicit response DTOs now cover inventory count/adjustment, sales/purchase history batches, supplier term POST/PUT, settings PUT, calendar PUT, and Purchase Order create/list/get/patch/confirm/receive. `GET /supplier-constraints`, inventory-constraint writes, legacy forecast/plan, and all R1.1–R1.6 boundaries remain unchanged.
- Completion contracts preserve current `MovementDelta` decimal strings; count's required empty `inventory` list; sales structured/scalar bounded JSON warnings; record-only purchase-batch `inventory_applied=false`; supplier write shape (intentionally without read-only `available_delivery_days`); settings budget fields; and calendar write counts/items.
- Purchase Order create retains `{orders:[...]}` supplier grouping. List now uses `Page[PurchaseOrderResponse]`; get/patch/confirm/receive share `PurchaseOrderResponse`. PO line order/received quantities remain strings, money remains integers, dates/datetimes retain their current serialization, and line ordering remains by `po_line_id`.
- State behavior is unchanged: `draft -> ordered` on confirm with budget reservation; `ordered -> partially_received -> received` on receipt; patch remains draft-only; receive retains lot/receipt/movement, expiry, idempotency, optimistic-version, and reserved-to-spent budget semantics.
- OpenAPI verification: 14 scoped success responses now reference explicit DTOs, including `Page_PurchaseOrderResponse_`; paths remain **58**, operations remain **70**. `bootstrap`, `dashboard`, and `GET /settings` remain intentionally untyped outside R1.7 scope.
- Verification: `tests/test_completion_response_contract.py` **3 passed**; full suite: **594 passed, 0 failed, 22 warnings in 614.56 seconds**. No Alembic migration, backfill, data cleanup, or SQLite compatibility cleanup was needed.
- Rollback is limited to `app/schemas/completion.py`, R1.7 route declaration hunks in `app/api/completion.py`, R1.7 assertions in `tests/test_operational_page_contract.py`, `tests/test_completion_response_contract.py`, and this checkpoint/plan documentation. Shared prior R1 WIP requires hunk-selective rollback. No data rollback is required.

## R1 phase-close review

- **CLOSED BY R1:** R1.1 common errors/OpenAPI; R1.2 Decision Package; R1.3 IngredientDemandRun; R1.4 ProcurementPlanRun; R1.5 Menu/Product; R1.6 operational pages; R1.7 Completion/Purchase Orders.
- **TECHNICAL DEBT OUTSIDE CURRENT R1 SCOPE:** bootstrap, dashboard, settings-read, import-result, and other lower-risk untyped read boundaries; broad nested Decision Package diagnostics; mixed repository/direct ORM use; `DecisionPlanningService` and `CompletionService` responsibility breadth.
- **PROPOSED FUTURE CHANGE:** cursor pagination, generic repository/UoW, CompletionService decomposition, PO aggregate/state-machine abstraction, and public DTO decoupling from legacy ORM-derived shapes.
- **R1 BLOCKER:** none. The approved R1 candidate inventory is complete; OpenAPI, route-manifest surface, tests, and contract gates are green.

## R2 entry review

- **Status:** COMPLETE (2026-09-09). This was inspection and planning only; no production code, route, OpenAPI, persistence, or runtime behavior changed.
- **Baseline verified:** canonical state is R1 complete/R2 not started; runtime OpenAPI remains **58 paths / 70 operations**; the full suite is **594 passed, 0 failed, 22 warnings in 607.39 seconds**. Existing working-tree changes are explained by completed R1 slices; no R2 production WIP was found.

| Task | Current default path | Optional / fallback path | Qwen called on default? | Business authority | Current caller |
|---|---|---|---|---|---|
| Excel Mapping | API builds a rule suggestion first; import pipeline returns it when confidence meets threshold | OpenRouter mapping; rule fallback when provider fails/unavailable | Import pipeline: no for high confidence. Direct `/llm/map-sheet`: **yes whenever provider is available** | Rule mapper / canonical schemas | `IngestionPipeline.suggest`, `POST /llm/map-sheet` |
| Strategy Explanation | Deterministic `strategy_evaluations` in `/brief` | `llm_polish`, whole-set deterministic fallback | No | backend selection/reason/evaluation projections | `DecisionPlanningService.get_decision_brief` |
| Ingredient Synthesis | Deterministic NORMAL/WATCH/CRITICAL, persisted after Decision Run commit | Per-item `llm_polish`; per-item deterministic fallback | No | `RiskDetail` + exact semantic evidence/provenance | summary enrichment; historical `/brief` reconstruction |
| Overall Summary | HYBRID guarded Qwen primary wording after core commit | Deterministic safe fallback; read-only missing-assistant fallback | Yes when recommendation and provider are available | persisted Decision Package + semantic facts | `_generate_and_persist_overall_summary`, `/brief` |
| Decision Explanation | Grounded Qwen over retrieved semantic evidence | Guarded deterministic provider fallback; service template fallback | Yes when provider available | immutable Decision Run facts/evidence | `POST /decision-runs/{id}/explanation` |
| What-if Explanation | Deterministic M1-M5 in-memory recomputation, then guarded Qwen wording | Deterministic what-if fallback | Yes when provider available | backend hypothetical package/comparison | `POST /decision-runs/{id}/what-if` |

- **Call-site/guard findings:** Strategy and Ingredient `llm_polish` are default-unused but reachable through typed settings and covered by tests; they are not dead. Overall Summary, Explanation, What-if, and Excel Mapping retain active provider callers. Semantic facts, communication plans, numeric/entity/provenance validation, exact CRITICAL evidence, and task-specific fallbacks are active authority/guard boundaries, not generic cleanup candidates.
- **Shared infrastructure finding:** Strategy, Ingredient, and Overall Summary use `app.llm.runtime.generate_json_sync`, which delegates to the gateway-owned event loop when available. Explanation and What-if each retain a local sync/async bridge around `generate_json`; this is confirmed duplication, but migration must preserve task, context, diagnostics, and fallback behavior.
- **Persistence finding under ADR-013:** old-row compatibility is not required, but `/brief` fallback for missing assistant data also covers current summary-enrichment failures and current public reads. It is therefore not classified old-disposable-only and is not a removal candidate without a separate contract gate.
- **Confirmed routing drift:** `IngestionPipeline.suggest` returns a high-confidence rule result without a provider call, while direct `POST /api/v1/llm/map-sheet` calls OpenRouter whenever available. The latter disagrees with accepted rule-first/ambiguity-only mapping intent. It is the first R2 candidate, not an entry-task fix.
- **RECOMMENDED FIRST R2 SLICE — R2.1:** align direct `POST /llm/map-sheet` with the existing `IngestionPipeline.suggest` high-confidence rule gate. Freeze `MappingSuggestion` (`sheet_type`, confidence, mapping, warnings/errors, `source`, `requires_review`, `raw_response`) and rule threshold semantics first. Preserve ambiguity/provider behavior, structured validation, safe provider-failure fallback, HTTP statuses, auth, and OpenAPI schema. Acceptance tests must prove: high-confidence API mapping makes zero provider calls and returns the canonical rule result; ambiguous mapping invokes the mapping task; provider failure preserves the existing safe fallback/503 split; import-pipeline parity; no effect on narrative task profiles. Rollback is limited to the API gate, focused mapping tests, and R2 documentation. Non-goals: gateway transport refactor, all-Qwen removal, mapping-schema redesign, and Narrative routing changes.
- **Other candidates, not started:** R2.2 shared Explanation/What-if sync-bridge consolidation (needs transport/diagnostic/fallback regression gate); R2.3 remove two proven unreachable legacy blocks (`_template_explanation`'s overwritten reason map and the post-`return` numeric branch) after fallback tests; deferred current-read assistant fallback cleanup; closed-intent deterministic Explanation routing requires a separately accepted behavior decision; deterministic-first Overall Summary and all-Qwen removal remain rejected under ADR-006.

## R2.1 — Direct `/llm/map-sheet` rule-first parity

- **Status:** COMPLETE (2026-09-09). Direct `POST /api/v1/llm/map-sheet` previously called the configured provider even when `map_sheet_rules` had already produced a high-confidence result. It now shares the import pipeline's pure `mapping_requires_llm` policy: high-confidence results return the canonical rule `MappingSuggestion` without a provider call; below-threshold results retain the existing `EXCEL_MAPPING` provider, validation, fallback, and error paths.
- **Contract/runtime evidence:** high-confidence responses retain the rule mapping, `source="rule"`, `requires_review=false`, and `raw_response=null`, whether a provider is configured or not. Ambiguous mappings still invoke the provider; a provider failure on a recognized rule result still returns the existing `rule_fallback` with review metadata. The route, status/auth/error behavior, confidence threshold, mapping schema, and generated OpenAPI `$ref` remain unchanged at **58 paths / 70 operations**.
- **Verification:** `tests/test_map_sheet_rule_first_parity.py` **3 passed**; focused OpenRouter map-sheet/provider-unavailable tests **4 passed**; full suite **597 passed, 0 failed, 22 warnings in 510.56 seconds**; `compileall` and `git diff --check` passed.
- **Persistence/rollback:** no migration, backfill, persistence, provider-lifecycle, or transport change. Roll back only the R2.1 hunks in `app/api/llm.py`, `app/core/rule_mapper.py`, `app/core/ingestion_pipeline.py`, `tests/test_map_sheet_rule_first_parity.py`, `tests/test_openrouter_provider.py`, and these documentation sections; prior R1 WIP in shared files remains hunk-selective. No data rollback is required.
- **Next candidate at R2.1 completion:** R2.2 shared Explanation/What-if sync-bridge consolidation; R2.3 remained not started.

## R2.2 — Explanation / What-if synchronous LLM invocation bridge

- **Status:** COMPLETE (2026-09-09). `DecisionNarrativeProvider` and `WhatIfNarrativeProvider` had separate local sync-to-async mechanics (`asyncio.run` plus a one-worker thread when a loop was already running). Both now use the existing `app.llm.runtime.generate_json_sync` gateway-aware bridge; What-if's local `_run_async` has been removed.
- **Preserved task boundary:** both calls retain `LLMTask.DECISION_NARRATIVE`, their unchanged prompts/payloads, request-local contexts (`decision_run_id`; What-if marker), task profile, retry/timeout, structured-output handling, owner-loop/client lifecycle, and raw diagnostics. The shared bridge delegates to a provider-owned `generate_json_sync` when present and retains the existing async-test-double fallback otherwise.
- **Task-specific behavior unchanged:** Explanation retains retrieval, communication plan, schema/entity/numeric/causal/citation grounding, and deterministic fallback. What-if retains deterministic M1-M5 recomputation before optional wording, its What-if-specific guard and deterministic fallback. Provider call count remains one for enabled successful calls and zero for unavailable paths.
- **Contract/gates:** Explanation remains `DecisionExplanationResponse`; What-if remains `WhatIfResponse`; OpenAPI remains **58 paths / 70 operations**. No route, public field, status, persistence, business computation, mapping, Strategy, Ingredient, or Overall Summary behavior changed.
- **Verification:** focused bridge/narrative/What-if/OpenRouter/Strategy/Ingredient/Summary/mapping tests **181 passed**; API/contract/What-if deterministic regression tests **18 passed**; full suite **599 passed, 0 failed, 22 warnings in 184.40 seconds**; `compileall` and `git diff --check` passed.
- **Persistence/rollback:** no migration, backfill, data cleanup, provider lifecycle change, or data rollback. Roll back only R2.2 hunks in `app/decision_intelligence/narrative.py`, `app/decision_intelligence/what_if_evidence.py`, `tests/test_llm_runtime.py`, `tests/test_decision_narrative.py`, `tests/test_what_if_narrative.py`, and these documentation sections. Existing R1/R2.1 shared-file WIP requires hunk-selective rollback.
- **Next candidate:** R2.3 remains not started and requires its own focused fallback-output gate.

## R2.3 — Proven unreachable narrative fallback cleanup

- **Status:** COMPLETE (2026-09-09). Two exact dead fragments were removed after current-code proof: the literal raw-reason map in `DecisionPlanningService._template_explanation` that was overwritten unconditionally by `mapping={}` before any read, and the legacy numeric parser block after an unconditional `return` in What-if `_validate_numbers` (including its now-unreferenced private parser).
- **Active fallback inventory preserved:** Strategy remains deterministic by default with whole-set polish fallback; Ingredient remains deterministic by default with per-item polish fallback and CRITICAL/LIMITED_EVIDENCE fail-closed behavior; Overall Summary retains HYBRID provider/schema/grounding deterministic-safe fallback; Explanation retains provider/validation/template fallback; What-if retains deterministic recomputation and narrative fallback; Excel Mapping retains rule-first ambiguity/provider safe fallback and unknown-unavailable `503` behavior. Current `/brief` persisted-assistant fallbacks remain current-runtime required and were not removed.
- **Proof and contract evidence:** neither removed fragment had a dynamic/config/DI caller. The reason-map literal was dominated by the immediate replacement; the numeric block was control-flow unreachable and its parser had no remaining reference. New focused tests freeze the active generic legacy-template output and the active authorized-display What-if numeric guard. Public response fields, source markers, warnings, diagnostics, raw responses, citations, task routing, and provider invocation policies are unchanged.
- **Verification:** fallback-focused Strategy/Ingredient/Summary/Explanation/What-if/Mapping/provider/bridge suite **183 passed**; Decision/API contract regression **17 passed**; OpenAPI remains **58 paths / 70 operations**; full suite **601 passed, 0 failed, 22 warnings in 178.70 seconds**; `compileall` and `git diff --check` passed.
- **Persistence/rollback:** no migration, backfill, persistence schema change, or data rollback. Roll back only R2.3 hunks in `app/services/decision_planning_service.py`, `app/decision_intelligence/what_if_evidence.py`, `tests/test_narrative_fallback_cleanup.py`, and these documentation sections; existing R1/R2.1/R2.2 WIP requires hunk-selective rollback.

## R2 phase-close review

- **CLOSED BY R2:** R2.1 direct map-sheet rule-first parity; R2.2 Explanation/What-if shared gateway-aware sync bridge; R2.3 exact unreachable fallback fragments.
- **TECHNICAL DEBT OUTSIDE CURRENT R2 SCOPE:** active transport/guard complexity and current `/brief` missing-assistant fallback behavior remain because they still protect active runtime paths.
- **PROPOSED FUTURE CHANGE:** remove old assistant fallback readers only after a separate current-read contract gate; broader narrative/provider simplification is not approved by this phase.
- **REQUIRES ADR / PRODUCT DECISION:** closed-intent deterministic Explanation routing. Deterministic-first Overall Summary and removing all Qwen remain rejected by current ADRs.
- **R2 BLOCKER:** none. The approved R2.1–R2.3 inventory is complete, contracts/OpenAPI remain stable, and the full suite is green. **R2 — DECISION INTELLIGENCE / NARRATIVE CLEANUP COMPLETE. R3 NOT STARTED.**

## Frozen invariants

- Backend owns business facts and recommendations; LLM may only word authorized evidence.
- New-item Opportunity Recommendation remains outside active-SKU M1–M6 paths.
- Selected, conservative-design, and stress scenarios retain separate authority.
- Strategy and Ingredient presentation are deterministic by default; Overall Summary remains hybrid.
- Cutoff D is an EOD boundary; future planning begins D+1.
- `fill_rate` is “tỷ lệ đáp ứng nhu cầu”; limitation/stress warnings are not silently converted into hard failures.
- Public contracts are preserved. Old disposable SQLite Decision Runs/JSON rows do not require compatibility, migration, or backfill (ADR-013).

## Current phase
**R3 COMPLETE. R3 ENTRY REVIEW, R3.1, R3.2, R3.3, R3.4, R3.5 and R3.6 are COMPLETE.**

**R1 — CONTRACT HARDENING COMPLETE. R2 — DECISION INTELLIGENCE / NARRATIVE CLEANUP COMPLETE.**

## R3 entry review — COMPLETE (2026-09-09)

- **State:** R3 implementation has not started. This was inspection/documentation only: no production code, route, OpenAPI, persistence, business formula, or ADR changed.
- **Verified authority:** forecast production is `ForecastService -> ExistingForecastCoreProvider -> shelfcash_core.train_forecast_core/predict_demand`; the `shelfcash_forecast` provider is optional/default-disabled shadow only. BOM/Ingredient Demand is `DecisionPlanningService.generate_demand -> CoreBomAdapter -> shelfcash_core.bom.engine`. Decision Run and What-if procurement use `CoreProcurementAdapter -> shelfcash_core` scenario composition, exact FEFO, optimizer, critic, risk and selection. Inbound expiry is core-owned in both the core adapter and CompletionService.
- **HISTORICAL R3 ENTRY FACT:** the typed `POST /stores/{store_id}/forecast-runs/{forecast_run_id}/procurement-plans` executes `ProcurementPlanningService.build()` and persists its application-layer planner result; it is a real production result, not a shadow. Core reuses that service only for ORM-state readers (`_lots`, `_terms`, `_open_inbound`). The former ADR-010 scope ambiguity is resolved by accepted ADR-014, which intentionally separates operational and Decision procurement authorities.
- **Package roles:** application code imports core for computation. It imports forecast only for the optional forecast shadow and Decision Intelligence `DecisionGraph`/`EvidencePackage`/evidence/retrieval helpers, which are contract/helper reuse rather than authority. There are no runtime core-to-forecast or forecast-to-core imports. Tests exercise core heavily; only forecast qualification compares the packages.
- **Parity/debt:** forecast is STRONG on its synthetic H1/H3/H7 qualification fixture (features, reconstruction, exact numerical output, calendar/crossing/short-history behavior) and shadow failure cannot alter persisted production rows. Cross-package BOM, Ingredient Demand, FEFO, Procurement, Strategy and Risk parity is NONE. Core FEFO, resimulation and strategy selection materially diverge from forecast equivalents; they are not merge-ready duplicates. Runtime forecast numerical drift is logged but not itself marked incompatible.
- **Frozen gates:** cutoff D is EOD and planning is D+1 through D+horizon; preserve units, float/Decimal boundaries, quantiles, MOQ/pack rounding, inclusive expiry, failures/warnings/reason codes and ADR-012 manager-safe semantics.
- **RECOMMENDED FIRST R3 SLICE — R3.1 Forecast production/shadow boundary hardening (READY):** freeze the provider/persistence boundary and add zero-history/cold-start plus public-response checks to the established forecast parity suite. Keep core production and forecast shadow isolated. Do not change algorithms, choose a package winner, share parity-critical helpers, remove packages, touch Decision Intelligence, or migrate the procurement endpoint.
- **Deferred:** package consolidation/shared-contract extraction, BOM/FEFO/procurement/strategy cleanup, and DecisionPlanningService decomposition. Procurement endpoint reconciliation needs semantic parity plus ADR scope review and is not R3.1.

## R3.1 - Forecast production / shadow boundary hardening - COMPLETE (2026-09-09)

- **Authority unchanged:** `ForecastService` continues to select `ExistingForecastCoreProvider -> shelfcash_core` as the only production provider. `ShelfCashForecastProvider -> shelfcash_forecast` remains optional/default-disabled shadow observation. Neither provider contains forecast business computation beyond delegation/contract translation.
- **Frozen boundary:** production reads canonical sales/calendar data through cutoff D, calls the core provider, persists `ForecastRunModel`/`ForecastPredictionModel`, and returns the existing typed deprecated `POST /api/v1/forecasts` response. Shadow is invoked only after the canonical transaction commits; it receives the same canonical input, cutoff and horizon, compares through current diagnostics, and cannot overwrite canonical rows or response data.
- **Parity evidence:** existing independently implemented H1/H3/H7 synthetic qualification remains exact (feature/reconstruction, calibration, quantiles, dates, calendar closure and warnings). Cold-start is the existing one-observation unseen product path; both providers match exactly and emit `UNSEEN_PRODUCT` plus `INSUFFICIENT_HISTORY`. Zero qualifying history is a frozen `DataValidationError` before prediction (not a zero forecast); both providers fail with the same boundary semantics. No shared forecast computation was extracted.
- **Non-interference/persistence/public evidence:** new HTTP acceptance coverage runs the same fixture shadow OFF and ON, proving identical canonical `ForecastResponse` fields and persisted run/prediction fields while only shadow-call count differs. Existing injected-shadow failure coverage proves core still completes, persists exactly one canonical run/prediction set, logs `forecast_shadow_inference_failed`, and leaves no shadow result/residual rows.
- **Contract and downstream preservation:** no API/OpenAPI/DTO/schema/migration change. OpenAPI/route checks remain **58 paths / 70 operations**. Forecast API/integration, Ingredient Demand, Planning, Decision, What-if and route/OpenAPI regressions pass without downstream code changes.
- **Verification:** forecast shadow/qualification tests **8 passed**; focused forecast/Decision/Planning/API regressions **18 passed**; OpenAPI/route tests **6 passed**; full suite **602 passed, 0 failed, 22 warnings in 163.49 seconds**; `python -m compileall -q app shelfcash_core shelfcash_forecast tests` and `git diff --check` passed.
- **Rollback:** R3.1-only hunks in `tests/test_forecast_shadow.py`, `tests/test_forecast_parity_qualification.py`, and these R3.1 documentation notes. No production code, data migration, backfill, persistence schema or data rollback is needed. Existing R1/R2 WIP remains hunk-selective.
- **Historical R3.1 candidate note:** procurement endpoint authority reconciliation was deferred for ADR-010 scope review. ADR-014 subsequently accepts the separate operational and Decision authorities; BOM/Ingredient Demand and FEFO/strategy parity remained separate candidates.

## R3.2 - Post-Forecast computation parity gate - COMPLETE (2026-09-09)

- **Authority map:** the typed Ingredient Demand endpoint is `DecisionPlanningService.generate_demand -> CoreBomAdapter -> shelfcash_core.bom.engine`; Decision Run and What-if are `CoreProcurementAdapter -> shelfcash_core` scenario/BOM, exact FEFO, optimization, critic, risk and selection. The typed Procurement Plans endpoint remains a separate production authority: `DecisionPlanningService.generate_plans -> ProcurementPlanningService.build -> InventorySimulationService`. `RecipeBomService` has no runtime production caller; it remains a legacy/test-only application implementation.
- **Characterization evidence:** a new independent package comparison freezes that core and forecast BOM agree on one-recipe quantiles, D+1 target date, unit and warnings, while the shadow loses `recipe_line_id` contribution provenance. This is a **SEMANTIC_DIVERGENCE** for public evidence, not a caller-migration proof. Core and forecast FEFO agree on known-expiry consumption; they deliberately diverge for core's `expiry_tracking_mode=not_required` FIFO/non-perishable semantics versus shadow's unknown-expiry warning. Core's optional `received_date` and shadow's required one are also a contract difference.
- **Post-Forecast matrix:** BOM numeric expansion is **ADAPTER_DIFFERENCE_ONLY** only when provenance is excluded, but full Ingredient Demand is **SEMANTIC_DIVERGENCE**; core/forecast unit helper modules are implementation-equivalent but app Decimal conversion remains a separate semantic boundary; FEFO/exact simulation, procurement, feasibility, strategy/risk are **SEMANTIC_DIVERGENCE**; scenario composer and ingredient-demand pipeline are package-equivalent implementation candidates but have no runtime alternate caller. No cross-package result is treated as a production truth.
- **HISTORICAL PROCUREMENT ADR FINDING:** R3.2 identified an ADR clarification need because the endpoint's operational planner owns persisted/public `ProcurementPlanRunResponse`, while Decision Run/What-if core owns a different selected-plan policy. The divergence evidence remains valid; ADR-014 now accepts the two authorities as distinct use-case contracts rather than a pending migration.
- **Selected next candidate (not started):** **R3.3 BOM shadow contribution-provenance parity hardening.** It is a shadow-only contract gap: retain core authority and independent computation, add the missing shadow recipe-line provenance and characterize missing-recipe/duplicate-line/unit-error parity before any caller question. It has no public route, persistence, or ADR change, and does not migrate `CoreBomAdapter` or touch procurement/FEFO/strategy.
- **Verification:** `tests/test_post_forecast_parity_characterization.py` plus existing BOM, inventory, procurement and strategy regressions: **45 passed, 1 warning in 13.51 seconds**. No production code, API/OpenAPI, DTO, persistence schema, algorithm, configuration or ADR changed. Existing R3.1 full-suite evidence remains **602 passed, 0 failed**.
- **Rollback:** R3.2-only hunks in `tests/test_post_forecast_parity_characterization.py` and these R3.2 documentation notes. No data rollback, migration, or production rollback applies.

## R3.3 - BOM shadow contribution-provenance parity hardening - COMPLETE (2026-09-09)

- **Authority unchanged:** production Ingredient Demand remains `DecisionPlanningService.generate_demand -> CoreBomAdapter -> shelfcash_core.bom.engine`. `shelfcash_forecast.bom` remains an independently implemented shadow/evidence path with no production caller, persistence, API or Decision Run authority.
- **Exact loss and repair:** shadow `adapt_recipes()` discarded an already-supplied optional `recipe_line_id` before building `RecipeRecord`; consequently `_build_source()` could not populate `IngredientDemandSource`. Shadow now preserves the supplied ID through its recipe contract, adapter and contribution output. It is never generated, hashed, indexed or inferred. For canonical ORM input it is the globally primary-keyed `RecipeLineModel.recipe_line_id` (`str`, non-null); it remains nullable only at the pre-existing pure-DataFrame boundary, exactly like core, for historical/non-persisted input that truly lacks a source identity.
- **Parity:** normal recipe, D+1 target, P25/P50/P75, unit, warnings and full source contribution including `recipe_line_id` now match. A direct two-line/same-ingredient fixture proves both engines retain two source contributions and their distinct IDs. Canonical persistence presently prevents that duplicate shape through `uq_recipe_line_ingredient`; the fixture freezes pure-engine behavior without inventing an unsupported ORM state. Missing active recipe produces matching `MISSING_RECIPE` issue/no contribution; incompatible product/yield units produce matching structured unit-conversion failure. No BOM formula, ordering, rounding or warning behavior changed.
- **Public/persistence/non-interference:** existing core API regression confirms `IngredientDemandRunResponse` still returns prediction quantiles as numbers and contribution evidence quantiles as strings, with the existing core `recipe_line_id`. Injecting a shadow BOM failure does not affect the production Ingredient Demand POST. Canonical `IngredientDemandRun`/prediction persistence, schema, DTO, routes and OpenAPI are unchanged; shadow data is not persisted.
- **Verification:** targeted BOM/API/persistence/Decision/narrative/OpenAPI regressions **24 passed, 1 warning in 14.26 seconds** before final public assertion update; full suite after the complete R3.3 change **608 passed, 0 failed, 22 warnings in 177.15 seconds**. OpenAPI remains **58 paths / 70 operations**. Compile and whitespace gates passed.
- **Rollback:** R3.3-only hunks in `shelfcash_forecast/bom/contracts.py`, `shelfcash_forecast/bom/adapter.py`, `shelfcash_forecast/bom/engine.py`, `tests/test_post_forecast_parity_characterization.py`, the R3.3 assertion hunk in `tests/test_planning_api.py`, and these notes. No data migration, backfill, schema or production-core rollback applies.
- **Historical R3.3 boundary:** FEFO semantic divergence, procurement authority clarification, and strategy/risk divergence were outside R3.3. R3.4 later resolved the procurement authority question through ADR-014.

## R3.4 - Procurement authority ADR clarification / migration gate review - COMPLETE (2026-09-09)

- **CURRENT FACT - split authority:** `POST /api/v1/stores/{store_id}/forecast-runs/{forecast_run_id}/procurement-plans` executes `DecisionPlanningService.generate_plans -> ProcurementPlanningService.build -> InventorySimulationService`, then persists `ProcurementPlanRun`, plans, lines, metrics and daily projections. Decision Run and What-if execute `CoreProcurementAdapter.optimize -> shelfcash_core` scenario construction, exact FEFO, optimizer, critic, feasibility/risk and selection. The core adapter reuses the legacy service only for `_lots`, `_terms` and `_open_inbound` ORM-state reads; it does not call `build()`.
- **Semantic delta:** the paths share the EOD inventory source and D+1 start, but are not output-equivalent. Legacy maps one quantile per named strategy, selects one supplier term before simulation, uses Decimal app-boundary rounding and the daily legacy simulator, resolves budget at cutoff D, evaluates store capacity, and recommends feasible `balanced` first. Core builds all supplier offers and design/risk scenarios, converts to core float contracts, resolves budget at decision D+1, validates with exact FEFO/critic, records store capacity as not evaluated when volume metadata is unavailable, and recommends the lowest exact-valid purchase cost then strategy name. MOQ/pack, calendar delivery and inclusive expiry are each tested in both implementations, but no common fixture proves wire-level equivalence.
- **Public/persistence risk:** R1.4 freezes `ProcurementPlanRunResponse`: run status/recommendation/warnings/failure, plan feasibility/cost/shortage/waste/fill/budget/metrics, lines (raw/rounded quantities, packs, MOQ, supplier/dates/reasons), and daily lot projections. `generate_plans` persists those exact legacy planner values in `ProcurementPlanRun`, `ProcurementPlan`, `ProcurementPlanLine`, `metrics_json` and `daily_projections_json`; core Decision package persistence is a different boundary. `/plan-runs` also transitively invokes `generate_plans`, so legacy compatibility callers depend on this behavior.
- **HISTORICAL R3.4 ADR conclusion:** before the user decision, **ADR CLARIFICATION NEEDED** and a caller replacement was **NOT SAFE TO MIGRATE**. ADR-014 subsequently accepts the intentional dual-authority scope; its semantic/public/persisted evidence remains the required baseline for any future convergence proposal.
- **Evidence/verification:** existing legacy/core/API/OpenAPI characterization regressions passed **77 passed, 15 warnings in 40.55 seconds**. They cover MOQ/pack, supplier/delivery, shelf life, inclusive expiry, capacity, budget/no-feasible, exact FEFO/risk, typed plan response, Decision/What-if and route contracts. No production code, algorithm, caller, DTO, OpenAPI, persistence schema, migration or ADR changed. The latest full-suite evidence remains R3.3: **608 passed, 0 failed, 22 warnings in 177.15 seconds** and **58 paths / 70 operations**.
- **ACCEPTED RESOLUTION:** user accepted Option B and ADR-014 records it. `/procurement-plans` is the Operational Procurement Authority; Decision Run/What-if are the Decision Procurement Authority. Their semantic divergence is intentional, not a global parity bug, and neither path is a shadow or temporary replacement for the other. The prior procurement ADR blocker is resolved.
- **CURRENT MIGRATION POLICY:** `/procurement-plans -> CoreProcurementAdapter` is **NOT CURRENTLY PLANNED**. Any future convergence needs a separately approved architecture and contract-migration decision covering semantics, public/persistence/client impact, characterization, migration and rollback. Duplicated implementation and maintenance burden remain technical debt; dual authority itself does not.

## R3.5 - Remaining computation duplication classification and safe-removal gate - COMPLETE (2026-09-09)

- **Scope and proof standard:** this was an inventory/caller-proof review only. The repository currently has 84 Python modules under `shelfcash_core` and 131 under `shelfcash_forecast`; 81 relative module paths overlap, of which 21 are byte-identical and 60 differ. Filename/content similarity was used only for discovery. A removal candidate had to prove zero runtime/config/public-export/Decision-Intelligence/test-oracle/CLI/dynamic-import/ADR role; no production module, caller, package layout, API, persistence, test behavior or ADR was changed.
- **Classified authorities and intentional independence:** core forecast/BOM and the Decision Run/What-if optimizer, FEFO, critic, risk and scenario path remain **PRODUCTION_AUTHORITY**. `ProcurementPlanningService` and its operational simulator remain the **ACCEPTED_ALTERNATE_AUTHORITY** for `/procurement-plans` under ADR-014. Forecast and BOM implementations in `shelfcash_forecast` remain **INTENTIONAL_SHADOW_PARITY**, including the R3.1 forecast qualification and R3.3 contribution-provenance oracle; neither is a removal candidate.
- **Active dependencies:** `app/services/forecast_service.py` config-selects the optional `ShelfCashForecastProvider` when `forecast_shadow_provider == "shelfcash_forecast"`. `app/decision_intelligence/adapter.py` actively imports forecast-package `DecisionGraph`, `EvidencePackage`, `EvidenceCollector`, `StructuredLocalRetriever` and `build_retrieval_context`. Importing that Decision-Intelligence package eagerly wires its agents, approval, computation gateway, regret and what-if exports, so those trees are **ACTIVE_RUNTIME_HELPER** in the current import graph, not dead merely because they lack a separate HTTP route.
- **Semantic alternates protected from cleanup:** core and forecast FEFO/inventory differ for `expiry_tracking_mode="not_required"`, unknown expiry, FIFO fallback and warning contracts; strategy/risk and optimizer semantics also differ from the core Decision path. They are **SEMANTIC_DIVERGENCE**, not mechanical deduplication candidates. The app Decimal unit boundary is an **ACTIVE_RUNTIME_HELPER**; core conversion is a production contract and forecast conversion remains part of its independent shadow/BOM path.
- **Unused versus dead:** forecast scenario exports and `decision_intelligence.evaluation` / `evaluation_part2` have no direct app/test/config caller found in the reviewed search roots. Scenario functions and both evaluation packages are nevertheless package-public exports, so they are only **PROVEN_UNUSED_CANDIDATE**, not **PROVEN_DEAD_DUPLICATE**. No module satisfies the full removal gate. `RecipeBomService` remains test-only/legacy and outside this cleanup scope.
- **Result and next-step policy:** **NO SAFE REMOVAL CANDIDATE.** There is no R3.6 deletion slice to start. Any later removal proposal must first establish supported-public-API status and a full caller/oracle proof for a leaf module; it must not merge packages, remove forecast/BOM shadow qualification, converge FEFO/strategy/risk, or change either ADR-014 procurement authority.
- **Verification:** direct import/caller/config/export searches plus shadow/BOM/Decision-Intelligence/route regression set: **38 passed, 1 warning in 14.82 seconds**. Runtime OpenAPI remains **58 paths / 70 operations**. No full suite was rerun because R3.5 changes documentation only; the latest full-suite evidence remains R3.3: **608 passed, 0 failed**. `git diff --check` passed after the documentation update.
- **Rollback:** only these R3.5 documentation hunks in `CURRENT_STATE.md` and `REFACTOR_PLAN.md`; no data, migration, production-code or package rollback applies.

## R3.6 - R3 exit review and residual computation debt handoff - COMPLETE (2026-09-09)

- **Exit decision:** **R3 COMPLETE. R3 BLOCKER: NONE.** The phase met its evidence objective without forcing a package merge, caller migration or dead-code removal: authority is explicit, independent parity paths are protected, material semantic differences are classified, and the procurement authority ambiguity was resolved by ADR-014.
- **Final authority map:** Forecast is `ForecastService -> ExistingForecastCoreProvider -> shelfcash_core`, with `ShelfCashForecastProvider -> shelfcash_forecast` as optional/default-disabled non-authoritative shadow. BOM and Ingredient Demand are `DecisionPlanningService.generate_demand -> CoreBomAdapter -> shelfcash_core.bom.engine`, with `shelfcash_forecast.bom` as an independent provenance-qualified shadow. Operational Procurement is `generate_plans -> ProcurementPlanningService.build -> InventorySimulationService`; Decision Procurement, FEFO, exact simulation, feasibility, strategy/risk and Decision/What-if scenarios are `CoreProcurementAdapter -> shelfcash_core`. Forecast scenario exports are public but currently unused candidates, not a production alternate authority.
- **Parity and non-interference closure:** forecast H1/H3/H7, zero-history and cold-start remain exact; shadow ON/OFF preserves canonical persisted/public output and a shadow failure is diagnostic only. BOM normal/duplicate-line/missing-recipe/unit-failure/D+1 provenance characterization remains current, including truthful `recipe_line_id`; canonical Ingredient Demand persistence and wire behavior remain core-owned. No later R3 slice changed either authority boundary.
- **Accepted divergence and residuals:** ADR-014 accepts Operational Procurement != Decision Procurement; `/procurement-plans -> CoreProcurementAdapter` is **NOT CURRENTLY PLANNED**. Core/forecast FEFO differences (non-perishable mode, unknown expiry, FIFO fallback and warnings) and strategy/risk differences (including selection/tie policy) are **SEMANTIC_DIVERGENCE**, not failed parity. Forecast/BOM independent implementations are **INTENTIONAL DUPLICATION**. Forecast scenario exports and Decision-Intelligence `evaluation` / `evaluation_part2` are **PROVEN_UNUSED_CANDIDATE**, not dead because they remain public exports. Active Decision-Intelligence imports remain `DecisionGraph`, `EvidencePackage`, `EvidenceCollector`, `StructuredLocalRetriever` and `build_retrieval_context`; no package deletion is safe.
- **Contract/persistence/narrative preservation:** OpenAPI remains **58 paths / 70 operations**; no route, DTO, response/status semantics, persistence schema, migration or backfill was introduced. Current persistence ownership remains `ForecastRun`, `IngredientDemandRun`, operational `ProcurementPlanRun`, and Decision `DecisionRun` at their existing paths. R2 Strategy Explanation, Ingredient Synthesis, HYBRID Overall Summary, Explanation, What-if narrative and Excel Mapping were not changed.
- **Exit verification:** representative R3 Forecast/BOM/Ingredient/operational-procurement/Decision-procurement/FEFO/strategy-risk/Decision/What-if/route regression set: **57 passed, 1 warning in 23.69 seconds**. Full suite: **608 passed, 0 failed, 22 warnings in 206.51 seconds**. `python -m compileall -q app shelfcash_core shelfcash_forecast tests`, runtime OpenAPI check and `git diff --check` passed. The only diff-check output was existing CRLF conversion warnings; no whitespace error was reported.
- **Handoff:** package consolidation is **DEFERRED**; shared parity-critical algorithm extraction is not a cleanup objective. Future public-API/caller review may reassess scenario/evaluation leaf exports; FEFO and strategy/risk convergence require a separately approved semantic change; procurement convergence requires a new ADR-level migration decision. R4 may begin only as **R4 ENTRY REVIEW — NOT STARTED**. `DecisionPlanningService` decomposition remains proposed, not accepted implementation architecture, and must preserve both procurement authorities, core BOM, Decision Package/Brief/What-if contracts, deterministic business authority and R2 narrative boundaries.
- **Rollback:** R3.6 is documentation-only. Revert only its hunk-selective entries in `CURRENT_STATE.md` and `REFACTOR_PLAN.md`; no production, data, migration or package rollback applies.

## R4 entry review - DecisionPlanningService use-case decomposition gate - COMPLETE (2026-09-09)

- **CURRENT FACT - service inventory:** `DecisionPlanningService` is an application facade constructed once by `app.main` with `(session_factory, settings, llm_provider)` and has no mutable cache or cross-request run state. It owns Ingredient Demand generate/read; Operational Procurement generate/read; active legacy `/plan-runs` create/read/result compatibility; Decision Run generation/read; Decision Brief construction; post-commit assistant enrichment; Explanation; What-if; plus forecast validation, idempotency/audit coordination and DTO/package serialization. It mixes direct ORM with `PlanningRepository`, adapters and specialized services; R4 does not change that R6-owned persistence detail.
- **Caller and authority map:** `app/api/planning.py` routes POST/GET Ingredient Demand, POST/GET Operational Procurement Plans, POST/GET Decision Runs, GET Brief, POST Explanation and POST What-if to the corresponding methods. `app/api/completion.py` routes active legacy POST/GET `/plan-runs` and `/result` to its compatibility methods. Ingredient Demand is `CoreBomAdapter -> shelfcash_core`; Operational Procurement is `ProcurementPlanningService.build -> InventorySimulationService`; Decision Run/What-if procurement is `CoreProcurementAdapter -> shelfcash_core`. The two procurement paths must remain explicitly separate under ADR-014.
- **Transaction facts:** Ingredient Demand commits its completed or blocked run in one service-owned session. Operational Procurement commits the running `ProcurementPlanRun` before calling the operational planner, then commits completed plans/lines or a blocked/failed status. Legacy plans persist their running compatibility row, delegate to demand/plans across their own sessions, then persist completion/failure and recommendations. Decision Run refreshes demand first, commits the deterministic `DecisionRun`/package, then separately calls `_generate_and_persist_overall_summary`; assistant failure is logged and cannot invalidate the committed Decision package. Reads do not commit. What-if constructs an in-memory hypothetical package and does not persist a `DecisionRun`.
- **Boundary/coupling findings:** `_forecast` and `PlanningRepository` are legitimate shared persistence/validation readers; `CoreBomAdapter`, `CoreProcurementAdapter`, `ProcurementPlanningService`, `_decision_package`, idempotency/audit and Decision Intelligence builders are authority-sensitive collaborators, not generic utilities. High coupling: `generate_decision` and `create_legacy_plan`; medium: Brief/Explanation/What-if due evidence/narrative composition; low: the individual run read methods. Optional LLM presentation is read-only for Brief/Explanation/What-if and R2 routing/fallback semantics remain frozen.
- **Proposed boundaries — migration guidance, not frozen layout:** candidate responsibilities are `GenerateIngredientDemand` / `ReadIngredientDemand`; `GenerateOperationalProcurementPlans` / `ReadOperationalProcurementPlans`; `GenerateDecisionRun` / `ReadDecisionPackage`; `BuildDecisionBrief`; `ExplainDecision`; and `EvaluateWhatIf`. ADR-015 accepts the focused-use-case direction, not these exact names or class count. Operational Procurement must never be named or combined with the internal Decision Procurement work in `GenerateDecisionRun`/`EvaluateWhatIf`. `DecisionPlanningService` may remain a facade while caller/compatibility proof requires it.
- **Accepted R4 direction:** ADR-015 accepts incremental decomposition into focused application use-case services. `DecisionPlanningService` may remain a temporary route/caller facade during migration; exact service names, count, final wiring and composition-root shape remain slice-driven rather than frozen.
- **R4 architecture gate:** **RESOLVED.** ADR-015 preserves ADR-007 public contracts, ADR-010 computation authority, ADR-014 Operational Procurement != Decision Procurement, active compatibility, R2 narrative routing, and current transaction/idempotency/failure semantics. In particular, deterministic `DecisionRun` persistence/commit remains before separate assistant enrichment persistence, and assistant failure cannot invalidate committed business truth. R4 implementation is eligible for controlled slices; no R4.1 work has started.
- **R4.1 candidate:** `ReadDecisionPackage` was the first low-risk candidate: read-only `get_decision`, frozen `DecisionPackage`, one session, no idempotency or computation, and hunk-selective facade rollback. It is completed below.
- **Contracts/persistence/narrative:** no route, DTO, OpenAPI, persistence schema, migration, UoW/repository policy, computation authority, procurement semantics or narrative routing changed. OpenAPI remains **58 paths / 70 operations**. Current frozen DTOs include `IngredientDemandRunResponse`, `ProcurementPlanRunResponse`, `DecisionPackage`, `DecisionBriefFacts`, `DecisionExplanationResponse`, `WhatIfResponse` and active legacy plan responses.
- **Verification:** targeted planning/procurement/inventory/Decision/Brief/Explanation/What-if/OpenAPI regressions: **40 passed, 1 warning in 62.42 seconds**. Latest full-suite evidence remains R3.6: **608 passed, 0 failed, 22 warnings in 206.51 seconds**. No production code or test code changed in R4 Entry.
- **Rollback:** R4 Entry is documentation-only. Revert only its hunk-selective `CURRENT_STATE.md` and `REFACTOR_PLAN.md` entries; no data, migration or production rollback applies.

### R4.1 — ReadDecisionPackage extraction — COMPLETE (2026-09-09)

- **Before / after boundary:** raw Decision Package read was implemented directly in `DecisionPlanningService.get_decision`: open a factory session, `session.get(DecisionRunModel, decision_run_id)`, raise `DECISION_RUN_NOT_FOUND` on absence, and `json.loads(run.package_json)`. It now delegates to `ReadDecisionPackage.read` in `app/services/decision/read_decision_package.py`. The existing facade method, route, signature, and callers remain unchanged.
- **Focused responsibility and dependencies:** `ReadDecisionPackage` reads exactly one persisted `DecisionRunModel` package by ID. It receives only the existing `session_factory`; it has no settings, LLM provider, computation adapter, planner, idempotency, or audit dependency. The read retains the existing current-runtime JSON decoding and missing-run `PlanningError` behavior without compatibility cleanup/redesign.
- **Contract / persistence preservation:** `GET /api/v1/decision-runs/{decision_run_id}` continues returning the same `DecisionPackage` response and the same 404 common-error envelope. The reader is read-only: no `add`, `flush`, `commit`, package rewrite, assistant generation, timestamp mutation, schema change, migration, or backfill. Brief, Explanation, What-if, Decision Run write/enrichment choreography, both procurement authorities, and R2 narrative routing are unchanged.
- **Verification:** new focused reader/facade/no-write/missing-route tests: **4 passed, 1 warning in 6.98s**. Full suite: **612 passed, 0 failed, 22 warnings in 736.20s**. `python -m compileall -q app shelfcash_core shelfcash_forecast tests` passed; runtime OpenAPI remains **58 paths / 70 operations**. `git diff --check` passed with only existing CRLF conversion warnings.
- **Rollback:** remove `app/services/decision/read_decision_package.py`, revert the `DecisionPlanningService` delegation/import hunk, the focused test file, and these R4.1 documentation hunks. No route, data, schema, migration, or persistence rollback exists.

### R4.2 — BuildDecisionBrief boundary characterization — COMPLETE (2026-09-09)

- **CURRENT FACT — runtime path and callers:** `GET /api/v1/decision-runs/{decision_run_id}/brief -> DecisionPlanningService.get_decision_brief`. The method is also called by `explain_decision`; no legacy route calls it. It opens one factory session, loads `DecisionRunModel`, preserves `DECISION_RUN_NOT_FOUND`, calls `DecisionBriefBuilder().build(session, run)`, applies only the configured presentation/fallback steps, and returns `DecisionBriefFacts`.

| Responsibility | Current owner | Classification |
|---|---|---|
| Package/model and related name/forecast reads; deterministic fact projection | `DecisionBriefBuilder` | READ PERSISTED DATA / PRESENTATION MAPPING; no business recomputation. |
| Risk, strategy comparison/evaluations, warnings, persisted assistant parsing | Decision Intelligence builders/projections | Deterministic presentation/fact selection. |
| Strategy Explanation | `strategy_presentation.present_all`; optional `StrategyExpressionProvider` | Deterministic default; `llm_polish` is read-only and validates or falls back as a whole set. |
| Overall Summary | post-commit `_generate_and_persist_overall_summary` + `OverallSummaryProvider` | HYBRID persisted enrichment; Brief consumes validated persisted data. |
| Ingredient Synthesis | post-commit enrichment + `IngredientSynthesisProvider` | Deterministic default, optional polish; exact RiskDetail evidence and fail-closed limited-evidence handling stay in the provider. |
| Missing assistant artifacts | `get_decision_brief` coordinating existing providers | CURRENT_COMPATIBILITY_REQUIRED read-time deterministic fallback; no token spend, no write/backfill. |
| Brief evidence / serialization | `ShelfCashDecisionIntelligenceAdapter.evidence_briefs` / `DecisionBriefFacts` | Read-only evidence projection / frozen DTO. |

- **Overall Summary lifecycle:** after deterministic Decision Run package commit, `_generate_and_persist_overall_summary` builds Brief facts and semantic facts, calls the HYBRID `OverallSummaryProvider.summarize` only if no valid persisted summary exists, persists the validated summary and synthesis in a separate commit, and never invalidates the Decision Run if it fails. `DecisionBriefBuilder` consumes valid persisted `assistant.overall_summary`; a missing/invalid value makes GET Brief return `OverallSummaryProvider.deterministic_fallback` only, with no mutation or provider call.
- **Persistence / LLM facts:** GET Brief is read-only: it performs no `add`, `flush`, or `commit`. Default strategy presentation calls no provider. Explicit `strategy_expression_mode="llm_polish"` may call `StrategyExpressionProvider`; guarded failure returns the deterministic presentation. Missing ingredient synthesis uses `IngredientSynthesisProvider(None, settings)`, so it cannot call a provider. No computation, procurement, FEFO, strategy-selection, or risk authority moves into the Brief path.
- **Boundary decision:** **APPLICATION_USE_CASE_BOUNDARY; EXTRACTION JUSTIFIED.** `DecisionBriefBuilder` is already the focused shared presentation/fact-projection collaborator (also used by post-commit enrichment and What-if), but `get_decision_brief` still owns a cohesive application intent: run/session/error coordination, presentation-mode routing, read-time compatibility fallback, and final evidence response composition. A future use case must delegate to those existing collaborators rather than absorb their algorithms.
- **ReadDecisionPackage relationship:** **NOT_NEEDED.** Brief requires the `DecisionRunModel` and related name/forecast reads in the same session; calling R4.1's raw JSON reader first would add a session and no current behavior/value. No reuse migration is implied.
- **Coupling / next candidate:** MEDIUM: one route plus Explanation use it, no write transaction, but it coordinates R2 presentation settings, an optional LLM polish path, persisted-artifact compatibility, and shared builders. R4.3 completed the extracted orchestration below.
- **Verification:** `tests/test_read_decision_package.py`, `test_decision_api.py`, `test_decision_intelligence.py`, `test_overall_summary.py`, `test_ingredient_synthesis.py`, `test_strategy_expression.py`, and `test_decision_assistant_contract.py`: **145 passed, 1 warning in 17.24s**. This R4.2 slice changed no production or test code. Latest full-suite evidence remains R4.1: **612 passed, 0 failed, 22 warnings in 736.20s**; it was not rerun for this characterization-only slice. OpenAPI remains **58 paths / 70 operations**.
- **Rollback:** R4.2 is documentation-only; revert only its hunk-selective `CURRENT_STATE.md` and `REFACTOR_PLAN.md` entries.

### R4.3 — BuildDecisionBrief extraction — COMPLETE (2026-09-09)

- **Before / after boundary:** `DecisionPlanningService.get_decision_brief` directly owned the factory session, `DecisionRunModel` load/not-found error, strategy-expression routing/logging, missing-assistant read-time fallback, and evidence composition. It now remains the stable facade and delegates to `BuildDecisionBrief.build` in `app/services/decision/build_decision_brief.py`, constructed with the current factory/settings/provider values so existing runtime provider/settings overrides retain their behavior.
- **Ownership / lifecycle:** `BuildDecisionBrief` owns exactly one read session, `DECISION_RUN_NOT_FOUND`, orchestration of existing collaborators, optional strategy polish, deterministic missing-summary/synthesis fallback, and final evidence attachment. `DecisionBriefBuilder` remains the deterministic package/ORM fact-projection owner. `StrategyExpressionProvider`, `OverallSummaryProvider`, `IngredientSynthesisProvider`, `DecisionSemanticEvidenceBuilder`, and `ShelfCashDecisionIntelligenceAdapter` retain their existing presentation, HYBRID-summary, synthesis, semantic-evidence, and fallback algorithms. There is no call to `ReadDecisionPackage`, no nested session, no computation move, and no narrative rewrite.
- **No-write / compatibility preservation:** GET Brief opens, reads, and closes its single session without `add`, `flush`, `commit`, package rewrite, assistant persistence, audit, idempotency, migration, or backfill. Missing/invalid summary remains an in-response deterministic fallback; missing synthesis remains provider-disabled deterministic synthesis. Default Strategy Explanation makes zero provider calls; explicit `llm_polish` keeps the validated whole-set fallback. Explanation continues calling the facade; What-if still uses the shared `DecisionBriefBuilder` unchanged.
- **Contract / authority preservation:** route, `DecisionBriefFacts`, errors/request ID, status, OpenAPI, Decision Package, deterministic business authority, Operational Procurement != Decision Procurement, Decision Run core-commit-before-enrichment, and R2 narrative architecture remain unchanged.
- **Verification:** new `tests/test_build_decision_brief.py` covers facade delegation, direct and HTTP missing-run errors, missing-artifact deterministic fallback, and package/timestamp non-mutation. Focused Brief/reader/API/Decision Intelligence/Overall Summary/Ingredient Synthesis/Strategy Expression/assistant-contract/What-if set: **150 passed, 1 warning in 20.43s**. Full suite: **616 passed, 0 failed, 22 warnings in 212.81s**. Compile passed; runtime OpenAPI remains **58 paths / 70 operations**; `git diff --check` passed with only existing CRLF conversion warnings.
- **Historical next-step note:** R4.3 left Ingredient Demand, Operational Procurement, active legacy plans, Decision Run generation and post-commit enrichment, raw Decision read facade, Brief facade, Explanation, What-if, and validation/idempotency/audit/shared coordination. R4.4 subsequently characterized those remaining responsibilities below; no heavy write-flow extraction was implied by R4.3.
- **Rollback:** remove `app/services/decision/build_decision_brief.py`, restore the former `get_decision_brief` body, remove its import and focused tests, and revert these R4.3 documentation hunks. No data, schema, migration, route, or OpenAPI rollback exists.

### R4.4 — Remaining responsibility / next-safe-boundary characterization — COMPLETE (2026-09-09)

- **CURRENT FACT — remaining ownership:** `get_decision` and `get_decision_brief` are now **FACADE_ONLY** delegates to `ReadDecisionPackage` and `BuildDecisionBrief`. Non-legacy ownership remaining in `DecisionPlanningService` is: Ingredient Demand command/query, Operational Procurement command/query, Decision Run generation plus post-commit assistant enrichment, Explanation, What-if, and shared `_forecast`, `_decision_package`, idempotency/audit/serialization coordination. `create_legacy_plan` and its reads remain active R5 compatibility work, not an R4 target.
- **Explanation characterization:** `POST /api/v1/decision-runs/{id}/explanation -> explain_decision` has one HTTP caller. It is read-only and opens no session itself, but intentionally coordinates the Brief facade (`BuildDecisionBrief` and its session), the raw package facade (`ReadDecisionPackage` and its session), immutable snapshot ingredient validation, `DecisionSemanticEvidenceBuilder`, `DecisionNarrativeProvider`, and the existing template fallback. The provider receives authorized facts only and may use `LLMTask.DECISION_NARRATIVE`; it cannot compute business truth. The ingredient-scoped branch preserves its current validation/error path; the unscoped branch preserves the current broad provider/read failure fallback. This is a coherent application use case, not a redundant wrapper around the narrative provider.
- **Write-flow characterization:** Ingredient Demand remains `CoreBomAdapter -> shelfcash_core` with `running -> completed` or `blocked` persistence, same-session idempotency registration/resource completion, audit record, and POST-to-GET response handoff; command/query stay together for migration safety. Operational Procurement remains ADR-014 Operational Procurement Authority through `ProcurementPlanningService -> InventorySimulationService`, with a committed `running` `ProcurementPlanRun` before build and separate terminal `completed`/`blocked`/`failed` commits, idempotency/audit, and frozen `ProcurementPlanRunResponse`; it is authority- and transaction-sensitive. Decision Run remains the highest-risk flow: its source-level idempotency registration/replay check occurs before refreshed demand, while the resource link is completed during final package persistence; it then performs refreshed core BOM demand, `CoreProcurementAdapter -> shelfcash_core`, immutable package persistence and deterministic commit, followed by separate best-effort Overall Summary/Ingredient Synthesis enrichment commit. Current targeted coverage protects the core/enrichment failure boundary but does not independently characterize the Decision Run idempotency replay path; that is a write-flow test gap, not a blocker for the selected read-only candidate. What-if remains one no-persistence application intent: baseline read, core hypothetical recomputation, shared Brief projection, guarded narrative and comparison.
- **Candidate risk matrix:** Explanation = **LOW transaction / LOW authority / MEDIUM narrative / LOW rollback**; Ingredient Demand = **MEDIUM transaction/idempotency / MEDIUM authority / MEDIUM rollback**; Operational Procurement = **HIGH transaction and ADR-014 authority / HIGH rollback**; Decision Run generation = **HIGH transaction/idempotency/narrative coupling / HIGH rollback**; What-if = **MEDIUM transaction-free but HIGH deterministic/narrative coupling / MEDIUM rollback**. Shared `_forecast` is an authority-sensitive state reader used by demand, operational procurement, Decision Run, What-if and legacy paths; `_decision_package` is Decision/What-if serialization and must remain shared for now; `_require_decision_ingredient` and `_template_explanation` are Explanation-specific candidates to move only with that use case.
- **Historical decision:** **R4.5 candidate — ExplainDecision extraction** was `READY_FOR_EXTRACTION` behind the existing facade because it has one clear read-oriented intent, a frozen `DecisionExplanationResponse`, no writes/idempotency/audit, no computation-authority move, focused existing coverage, and a small rollback. It was completed below with the separate Brief/raw-read lifecycle, snapshot ingredient validation, `LLMTask.DECISION_NARRATIVE`, provider/template fallback and HTTP errors preserved. ADR-015 remained sufficient; no new ADR was required.
- **Verification / non-goals:** targeted Brief/raw-read/Decision API/Decision Intelligence/Ingredient Demand/Operational Procurement/Decision Run/Overall Summary/Ingredient Synthesis/Strategy Expression/What-if/Explanation regression: **166 passed, 1 warning in 22.30s**. R4.4 changed no production or test code. Latest full-suite evidence remains R4.3: **616 passed, 0 failed, 22 warnings in 212.81s**. OpenAPI remains **58 paths / 70 operations**. No DTO, route, schema, persistence, transaction, computation, procurement, FEFO, narrative, or legacy compatibility behavior changed.
- **Rollback:** R4.4 is documentation-only; revert only these hunk-selective `CURRENT_STATE.md` and `REFACTOR_PLAN.md` entries. R4.5 is documented below.

### R4.5 — ExplainDecision extraction — COMPLETE (2026-09-09)

- **Before / after:** `DecisionPlanningService.explain_decision` directly owned Brief/raw-package coordination, ingredient-in-snapshot validation, semantic-evidence preparation, narrative provider invocation, and template fallback. It now remains the stable facade and delegates to `ExplainDecision.explain` in `app/services/decision/explain_decision.py`. The facade creates the focused boundary using the current `BuildDecisionBrief`, `ReadDecisionPackage`, settings, and LLM provider values; no route or composition-root rewiring occurred.
- **Focused ownership:** `ExplainDecision` owns only read-oriented explanation orchestration and the exclusively Explanation-specific `_require_decision_ingredient` / `_template_explanation` helpers. It calls `BuildDecisionBrief` and `ReadDecisionPackage` directly, preserving their existing independent sessions and avoiding a facade cycle. `DecisionNarrativeProvider`, `DecisionSemanticEvidenceBuilder`, and existing Decision Intelligence algorithms remain their current owners; no business computation, prompt, provider, or fallback algorithm moved.
- **Frozen behavior:** valid ingredient scope remains constrained to the immutable Decision Run snapshot and preserves `DECISION_RUN_INGREDIENT_NOT_FOUND` (`422`) semantics. Semantic evidence still derives from the Brief plus raw persisted package. The provider still uses `LLMTask.DECISION_NARRATIVE`; unavailable, invalid, or failing provider behavior keeps the existing deterministic/provider and template fallback paths. Explanation remains read-only: no commit, flush, mutation, assistant persistence, package rewrite, audit, idempotency, migration, or backfill. Brief, raw package read, What-if, Strategy Explanation, Ingredient Synthesis, Overall Summary, procurement authorities, and Decision Run commit-before-enrichment behavior are unchanged.
- **Contract / verification:** route, request DTO, `DecisionExplanationResponse`, status/error/request-ID envelope, and OpenAPI remain unchanged at **58 paths / 70 operations**. New `tests/test_explain_decision.py` covers facade delegation, direct and HTTP missing-run behavior, and package/timestamp non-mutation through the read-only fallback path; existing ingredient-scope, narrative task/provider/fallback, Brief, raw-read and What-if regressions remain green. Focused regression: **188 passed, 1 warning in 21.61s**. Full suite: **620 passed, 0 failed, 22 warnings in 171.75s**. Compile passed; `git diff --check` passed with only existing CRLF conversion warnings.
- **Historical next-step note:** real ownership remains Ingredient Demand, Operational Procurement, Decision Run generation/enrichment, What-if, shared validation/idempotency/audit/serialization, and R5 legacy compatibility. `get_decision`, `get_decision_brief`, and `explain_decision` are facade-only. R4.6 records the phase-close classification below; no write-flow extraction was implied by R4.5.
- **Rollback:** remove `app/services/decision/explain_decision.py`, restore the former `explain_decision` body and its two private helpers, remove focused tests, and revert these R4.5 documentation hunks. No data, schema, migration, route, or OpenAPI rollback exists.

### R4.6 — R4 Exit Review / Remaining Write-Flow Handoff — COMPLETE (2026-09-09)

- **Exit decision:** **R4 COMPLETE. R4 BLOCKER: NONE.** ADR-015 was applied as an incremental, evidence-led decomposition—not as a requirement to empty or delete `DecisionPlanningService`. The low-risk boundaries with a clear application intent are now extracted; no remaining responsibility satisfies the same useful low-transaction, low-authority, small-rollback threshold.
- **Final ownership map:** `ReadDecisionPackage` is the **EXTRACTED_OWNER** for raw persisted Decision Package reads; `BuildDecisionBrief` is the **EXTRACTED_OWNER** for one-session, read-only Brief orchestration; and `ExplainDecision` is the **EXTRACTED_OWNER** for read-only Explanation orchestration. `DecisionPlanningService.get_decision`, `.get_decision_brief`, and `.explain_decision` are **FACADE_ONLY** delegates. This temporary facade remains accepted under ADR-015 and has no duplicate active implementation behind it.

| Residual responsibility | Current owner / classification | Why retained and handoff |
|---|---|---|
| Ingredient Demand command/query | `DecisionPlanningService` — **RETAINED_WRITE_FLOW** | `CoreBomAdapter -> shelfcash_core` authority, `running`/`completed`/`blocked` lifecycle, same-session idempotency resource completion, audit, and POST-to-GET continuity remain one migration-sensitive flow. Future application write-flow review before any extraction. |
| Operational Procurement command/query | `DecisionPlanningService` plus `ProcurementPlanningService -> InventorySimulationService` — **RETAINED_WRITE_FLOW**, authority-sensitive | ADR-014 operational authority, pre-build committed `running` `ProcurementPlanRun`, terminal success/failure commits, idempotency/audit, and public persisted plan semantics must remain intact. No migration to `CoreProcurementAdapter`; future write-flow review only. |
| Decision Run generation/enrichment | `DecisionPlanningService` — **RETAINED_COHERENT_USE_CASE** | Refreshed demand and core decision computation lead to deterministic package persistence/commit, then separate best-effort assistant enrichment persistence. Keep this commit choreography coherent; independently characterize idempotency replay before any future write extraction. |
| What-if | `DecisionPlanningService` — **RETAINED_COHERENT_USE_CASE** | Baseline read, in-memory deterministic recomputation, shared Brief projection, comparison, and guarded narrative form one no-hypothetical-persistence intent. |
| `_forecast`, `_decision_package`, idempotency/audit/serialization | `DecisionPlanningService` — **SHARED_COORDINATION** | Authority-sensitive state reading and write-flow coordination; not generic utility or repository/UoW extraction targets. Persistence boundary review remains R6 work. |
| Legacy `/plan-runs` compatibility | `DecisionPlanningService` — **R5_LEGACY** | Active compatibility behavior is handed to R5 entry review; no removal or lifecycle change was made in R4. |

- **Residual test gap:** Decision Run idempotency replay still lacks an independent characterization test. It is a **TEST GAP / future write-flow prerequisite**, not an R4 exit blocker: R4 did not authorize or leave half-completed a Decision Run write extraction.
- **Preservation gates:** Forecast production remains `shelfcash_core`; Ingredient Demand remains `CoreBomAdapter -> shelfcash_core`; Operational Procurement remains the separate ADR-014 `ProcurementPlanningService -> InventorySimulationService` authority; Decision procurement/FEFO/feasibility/strategy/risk remain `CoreProcurementAdapter -> shelfcash_core`. R2 narrative routing is unchanged: deterministic Strategy Explanation and Ingredient Synthesis defaults with optional polish, HYBRID Overall Summary, and guarded narrative Explanation/What-if. Ingredient Demand lifecycle, Operational Procurement multi-commit lifecycle, deterministic Decision Run commit before separate enrichment, What-if no-persistence, and Brief/Explanation read-only behavior are unchanged.
- **Contract / persistence:** no route, DTO, status, error, OpenAPI, schema, migration, backfill, repository, or Unit-of-Work change. Runtime OpenAPI remains **58 paths / 70 operations**. `DecisionPackage`, `DecisionBriefFacts`, `DecisionExplanationResponse`, `IngredientDemandRunResponse`, `ProcurementPlanRunResponse`, `WhatIfResponse`, and legacy contracts remain unchanged.
- **Verification:** targeted extracted-boundary and retained-flow regression (`ReadDecisionPackage`, `BuildDecisionBrief`, `ExplainDecision`, Decision API/Intelligence, Ingredient Demand/Operational Procurement, Overall Summary, assistant contract, What-if, narrative fallback) **100 passed, 1 warning in 25.10s**. Full suite **620 passed, 0 failed, 22 warnings in 170.29s**. `python -m compileall -q app shelfcash_core shelfcash_forecast tests` and runtime OpenAPI checks passed. `git diff --check` passed after this documentation update. Existing unrelated WIP remains preserved.
- **Historical rollback / next-phase note:** R4.6 is documentation-only and hunk-selective. Revert its block and matching plan block to reopen the phase decision; no data rollback exists. R5 Entry Review is completed below; R6 remains the later persistence/session/repository/UoW boundary review.

## R5 Entry Review — Legacy Compatibility Isolation & Removal Gate — COMPLETE (2026-09-10)

- **Preflight / current fact:** R1–R4 remain complete; R4 closed with no blocker. R5 Entry made no production, route, DTO, OpenAPI, persistence, or ADR change. The runtime OpenAPI baseline remains **58 paths / 70 operations**.

| Legacy or compatibility surface | Current handler / public status | Classification | Caller evidence |
|---|---|---|---|
| `POST`, `GET`, and `GET /result` `/api/v1/stores/{store_id}/plan-runs` | `completion.py` -> `DecisionPlanningService.create_legacy_plan`, `.get_legacy_plan_metadata`, `.get_legacy_plan_result`; OpenAPI-visible, not deprecated | **CURRENT_COMPATIBILITY_REQUIRED** | HTTP route, three typed `LegacyPlan*` contracts, route manifest, integration/idempotency/error tests; no in-repo frontend/SDK client found, so external callers remain unknown. |
| Store-scoped forecast-run POST/metadata/result | `completion.py` -> `ForecastService.create_legacy_run`, `.get_metadata`, `.get_legacy_result`; OpenAPI-visible, not deprecated | **CURRENT_COMPATIBILITY_REQUIRED** | HTTP route, typed legacy forecast contracts, route manifest, integration/fallback tests; unknown external callers. |
| `POST`/`GET /api/v1/forecasts` | `forecast.py` -> `ForecastService.predict/get`; OpenAPI-visible with `deprecated=true` | **DEPRECATED_BUT_ACTIVE** | HTTP route, route manifest, Forecast/shadow/error contract tests; unknown external callers. |
| `ImportModel` legacy-row bridge (`sync_legacy`, `get_legacy`, `_backfill_legacy`) | current ImportService/repository runtime path | **DEPRECATED_BUT_ACTIVE** | current imports still call `sync_legacy`; current read fallback calls `get_legacy`; no removal proof. |
| `/brief` missing-assistant deterministic fallback | `BuildDecisionBrief` and existing Decision Intelligence collaborators; canonical Brief route | **CURRENT_COMPATIBILITY_REQUIRED** / **OUT_OF_SCOPE** | protects current enrichment-failure reads, not merely old rows; Decision Intelligence/fallback tests cover it. |
| `ImportBusinessPersistenceService._persist_supplier_constraints_legacy` | private, unexported leaf helper | **PROVEN_DEAD** | exact whole-repo search finds only its definition; dynamic dispatch derives handlers only from canonical `sheet_type` values, which include `supplier_constraints` but not `supplier_constraints_legacy`; no route, config, script, test oracle, persistence write, or ADR role. |
| `LegacySupplierInventoryValueModel` / migration archive table | package-exported migration archive model | **PROVEN_UNUSED_CANDIDATE** | no operational runtime caller found, but model is exported and migration/table history exists; do not remove without a separate public-package/persistence review. |

- **Legacy plan lifecycle / authority:** legacy plan POST validates mapped strategy and cutoff, registers the `/plan-runs` idempotency record, persists a `PlanRunModel(status=running)`, then calls the current Ingredient Demand and **Operational Procurement** flows. It commits the compatibility row before nested work; on success it links the resulting `ProcurementPlanRun`, writes `RecommendationModel` rows and `legacy_plan_generated` audit, then commits `completed`; planning errors persist `blocked`, unexpected errors persist `failed`. Metadata reads `PlanRunModel`; result reads the linked canonical `ProcurementPlanRun` and recommendations without re-running BOM or the planner. This is an adapter over current canonical flows, not a third procurement authority: supplier/FEFO/procurement behavior remains `ProcurementPlanningService -> InventorySimulationService` under ADR-014.
- **Frozen legacy errors/contracts:** invalid legacy strategy or cutoff uses `FORECAST_INPUT_INVALID`; non-completed forecast rejects before a legacy row is created; missing/wrong-store plan is `PLANNING_RUN_NOT_FOUND` (404); non-completed result returns its persisted failure code or `PROCUREMENT_PLAN_INFEASIBLE` (409); a missing selected canonical plan is `PLANNING_PERSISTENCE_INCONSISTENCY` (500). POST retains `Idempotency-Key` replay/conflict behavior; all three routes require the normal API-key dependency and expose current typed metadata/result DTOs.
- **Historical versus current compatibility:** ADR-013 permits dropping code whose only purpose is old disposable SQLite rows; it does not authorize removal of current routes, current row lifecycles, current client contracts, or current enrichment-failure fallback. Thus historical import-row/backfill and migration-archive evidence alone is not a public-route blocker, while the current public plan/forecast surfaces remain protected by ADR-007.
- **Candidate decisions:** **SAFE REMOVAL CANDIDATE FOUND — `ImportBusinessPersistenceService._persist_supplier_constraints_legacy`.** It is a single private duplicate leaf with all caller-proof gates met. **LEGACY BOUNDARY EXTRACTION JUSTIFIED** for `/plan-runs` as a later behavior-preserving isolation option: it would materially separate active compatibility ownership from `DecisionPlanningService`, but its multi-commit/idempotency/public-contract lifecycle is not the smallest next change. Public legacy route deprecation or removal is not approved: it requires a separate **NEW ADR REQUIRED** public-contract/deprecation decision because routes are OpenAPI-visible, two route families lack deprecation metadata, and external-client telemetry is absent.

| Candidate | Contract risk | Transaction / authority risk | Caller uncertainty | Rollback | Disposition |
|---|---|---|---|---|---|
| Remove proven-dead supplier-constraint legacy helper | LOW | LOW | LOW | LOW | Selected R5.1 candidate. |
| Extract `LegacyPlanCompatibility` behind the existing facade | LOW if behavior-preserving | HIGH: legacy row plus nested canonical commits and ADR-014 | MEDIUM: public external callers unknown | MEDIUM | Justified future isolation; not selected before the smaller dead leaf. |
| Deprecate/remove public plan or forecast compatibility routes | HIGH | HIGH | HIGH | HIGH | Blocked pending explicit public API decision. |

- **R5.1 recommendation:** **R5.1 — Remove `ImportBusinessPersistenceService._persist_supplier_constraints_legacy`.** Scope: only that private method and focused proof that canonical dynamic dispatch still resolves `_persist_supplier_constraints`; no model/table/migration/export removal, no route change, no import semantic change, and no legacy plan/forecast work. ADR conclusion for this internal dead-leaf removal: **NO ADR NEEDED.** Rollback restores the single method; no data rollback applies.
- **Verification:** legacy plan create/metadata/result/idempotency/error, nested canonical planning, deprecated forecast, active import bridge, Brief fallback, planning and OpenAPI/error regression: **58 passed, 1 warning in 107.42s**. Full-suite result was not rerun for this documentation-only entry review; latest R4.6 baseline is **620 passed, 0 failed, 22 warnings**. `git diff --check` passed after the documentation update. Existing unrelated WIP remains preserved.

## R5.1 — Remove Proven-Dead Supplier-Constraint Helper — COMPLETE (2026-09-10)

- **Before / deadness proof:** `app/services/business_persistence.py` defined private, unexported `ImportBusinessPersistenceService._persist_supplier_constraints_legacy`. Immediate whole-repository proof found no runtime, route, OpenAPI, test, config/dynamic-import, script/CLI, or package-export reference. Canonical dynamic dispatch can select `_persist_supplier_constraints` from the `supplier_constraints` sheet type, never the legacy suffix. The removed leaf would have written `SupplierIngredientTermModel`/`ImportIssueModel` rows only if called; no current persistence lifecycle reaches it.
- **Removal and preservation:** only `_persist_supplier_constraints_legacy` was removed; no same-file import became unused. The active `_persist_supplier_constraints` import path, `ImportModel` legacy-row `sync_legacy`/`get_legacy`/`_backfill_legacy` bridge, `/plan-runs`, store forecast-run routes, deprecated-but-active `/forecasts`, and current `/brief` missing-assistant fallback are unchanged. `LegacySupplierInventoryValueModel` remains **PROVEN_UNUSED_CANDIDATE**, not removed; no model, migration, table, schema, DTO, route, or OpenAPI change occurred.
- **Verification / rollback:** post-removal search found **0 non-documentation references**. Focused import/compatibility regression: **112 passed, 15 warnings in 69.92s**. Full suite: **620 passed, 0 failed, 22 warnings in 175.05s**. `compileall` passed. Rollback restores this one method only; no data rollback applies. The public legacy-route removal blocker remains **PUBLIC_API_DECISION_NEEDED**. **R5.2 — LegacyPlanCompatibility Boundary Characterization** is recommended but not started.

## R5.2 — LegacyPlanCompatibility Boundary Characterization — COMPLETE (2026-09-10)

- **CURRENT FACT — public compatibility path:** `POST /api/v1/stores/{store_id}/plan-runs` accepts current public `PlanIn`, uses the `Idempotency-Key` header and returns `LegacyPlanMetadataResponse`; `GET /plan-runs/{plan_run_id}` returns that metadata; and `GET /plan-runs/{plan_run_id}/result` returns `LegacyPlanResultResponse`. All three routes are API-key protected, OpenAPI-visible, and not deprecated. They remain **CURRENT_COMPATIBILITY_REQUIRED**; no in-repository consumer proves external callers absent.
- **Command / transaction timeline:** `DecisionPlanningService.create_legacy_plan` maps strategy and validates forecast/cutoff, registers its legacy POST idempotency record, creates `PlanRunModel(status=running)` and commits it with the idempotency resource link. It then probes demand in a separate session, calls current `generate_demand` only when no completed demand exists, calls current `generate_plans` for the mapped operational strategy, and finally opens a third compatibility session to link the resulting `ProcurementPlanRun`, persist positive supplier-line `RecommendationModel` projections, write `legacy_plan_generated`, set `PlanRun` completed, and commit. A `PlanningError` after the initial commit marks the PlanRun `blocked` with its exact code/message in a fresh session; an unexpected exception marks it `failed` with `PROCUREMENT_PLAN_INFEASIBLE`. Failure before the initial commit (invalid strategy, forecast, or cutoff) creates no PlanRun. A failure while writing the final projection/audit rolls back that final session before the failure-status session runs.

| Stage | Persisted state / commit | Failure consequence |
|---|---|---|
| Initial compatibility write | idempotency record + `PlanRun(running)` + resource link; **commit** | validation before this point leaves no PlanRun. |
| Nested demand | current `IngredientDemandRun` lifecycle in its own session | legacy PlanRun becomes `blocked`; BOM authority remains canonical. |
| Nested procurement | current `ProcurementPlanRun(running)` then terminal lifecycle in its own session | legacy PlanRun becomes `blocked`/`failed`; operational authority remains canonical. |
| Compatibility finalization | PlanRun link, recommendations, success audit, completed status; **one commit** | finalization failure rolls back this group, then attempts PlanRun `failed` persistence. |

- **Idempotency / audit / read facts:** idempotency is a legacy-owned wrapper using the shared `IdempotencyService`; nested demand/procurement are called without nested keys. An identical replay returns `get_legacy_plan_metadata` for the already linked resource—even a `blocked` run—while a different payload/key hash remains `IDEMPOTENCY_KEY_REUSED` (409). There is no separate in-progress conflict response: a concurrent replay can receive current metadata. Successful compatibility finalization writes exactly `legacy_plan_generated` (`plan_run`, `planning_service`) in the same final commit; canonical demand/procurement write their own audits; no legacy failure audit is emitted. Metadata is a direct PlanRun read; result reads metadata, the linked operational plan through `get_plans`, then Recommendation/ingredient/supplier lookups without re-running BOM or the planner.
- **Authority / compatibility ownership:** Ingredient Demand remains `CoreBomAdapter -> shelfcash_core`; Operational Procurement remains `ProcurementPlanningService -> InventorySimulationService` under ADR-014. Legacy owns strategy mapping, PlanRun lifecycle, legacy idempotency/replay, compatibility recommendation projection, audit naming, and metadata/result DTO composition. It owns no BOM/procurement computation. `RecommendationModel` is a compatibility projection from the selected operational plan, but is also consumed by current Purchase Order creation; it cannot be treated as an isolated throwaway table.
- **Boundary decision:** **EXTRACTION JUSTIFIED BUT REQUIRES IMPLEMENTATION GATES.** The coherent intent is “serve the current PlanRun compatibility contract while delegating business work to canonical authorities.” A future boundary must preserve all three commit groups, exact replay-to-metadata behavior, failure states, audit/recommendation atomicity, store scoping, and current Completion/Purchase Order reads. It must use one-way explicit collaborators for demand, operational procurement, and plan reads; `LegacyPlanCompatibility -> DecisionPlanningService` would create an unacceptable facade cycle. Internal behavior-preserving extraction needs **NO ADR NEEDED** under ADR-008/ADR-015; public `/plan-runs` removal remains **NEW ADR REQUIRED**. **R5.3 — LegacyPlanCompatibility Extraction** is recommended, not started.
- **Verification / non-goals:** added focused characterization coverage for committed-running/failure/idempotency replay and recommendation/audit persistence. `tests/test_planning_api.py`, `tests/test_forecast_integration.py`, `tests/test_completion_behavior.py`, and `tests/test_error_openapi_contract.py`: **22 passed, 1 warning in 20.36s**. No production code, route, DTO, OpenAPI, schema, migration, backfill, computation, procurement authority, or narrative behavior changed. Latest full-suite evidence remains R5.1: **620 passed, 0 failed, 22 warnings**.

## R5.3 — Canonical Write-Flow Collaborator Prerequisite Characterization — COMPLETE (2026-09-10)

- **Re-scoped migration evidence:** an attempted direct `LegacyPlanCompatibility` extraction was blocked before production change. The proposed `LegacyPlanCompatibility -> DecisionPlanningService.generate_demand/generate_plans/get_plans` collaboration would create `DecisionPlanningService -> LegacyPlanCompatibility -> DecisionPlanningService`; copying those methods would duplicate the active write flows. `CoreBomAdapter` and `ProcurementPlanningService.build()` are computation collaborators only, not the full current application lifecycles. The prerequisite is now explicit: one-way Ingredient Demand and Operational Procurement lifecycle collaborators must exist before the legacy boundary is retried.
- **Frozen public surface:** the API remains **58 paths / 70 operations**. All four routes are API-key protected, OpenAPI-visible, and non-deprecated: `POST`/`GET /api/v1/stores/{store_id}/forecast-runs/{forecast_run_id}/ingredient-demand` (POST `Idempotency-Key`, `IngredientDemandRunResponse`) and `POST`/`GET /api/v1/stores/{store_id}/forecast-runs/{forecast_run_id}/procurement-plans` (POST `ProcurementPlansRequest` plus `Idempotency-Key`, `ProcurementPlanRunResponse`). Success remains 200; current 401 auth, generated 422 validation, store scope, and `PlanningError` status/code behavior are preserved.

### Ingredient Demand

- **Current lifecycle:** `DecisionPlanningService.generate_demand` uses `_forecast` to validate the store, completed forecast, and predictions; registers path/forecast-hash idempotency; reuses completed demand unless internal-only `refresh=True`; flushes a new `IngredientDemandRun(running)`; calls `CoreBomAdapter(session).expand() -> shelfcash_core`; then persists prediction rows, completed status/warnings, `ingredient_demand_generated`, successful idempotency resource link, and one terminal commit. A `PlanningError` commits `blocked` with its exact code/message and completion time; no success audit is written. An unexpected exception has no explicit commit, so the uncommitted session work rolls back on close. `get_demand` repeats completed-forecast/store validation, reads the run and ordered predictions, and maps the current DTO; completed-without-predictions is 500 except for `INGREDIENT_SCOPE_NO_MATCH`.

| Stage | Write | Commit | Failure state |
|---|---|---:|---|
| Validation/idempotency/existing lookup | possible idempotency `flush` | no | pre-terminal validation leaves no newly committed work. |
| Running/refresh preparation | run `flush`, or prediction delete/reset | no | unexpected error rolls it back. |
| Success | predictions, completed status, audit, successful key link | yes | no partial terminal success. |
| `PlanningError` | blocked status/code/message/completed time | yes | original error response; no success audit. |

- **Idempotency/audit:** scope is `(store, exact POST path, method, key)`; the hash contains only `forecast_run_id`, so a distinct public payload cannot exist. Success links `ingredient_demand_run` and status 200; same-key replay rolls back the replay-session registration and returns `get_demand`. A blocked keyed request commits its key without resource link or response status, but replay still returns its forecast-scoped blocked representation. Success audit is exactly `ingredient_demand_generated`, resource type `ingredient_demand_run`, source `planning_service`, and `{forecast_run_id, prediction_count}` in the same terminal transaction.
- **Dependencies / verdict:** `_forecast` is the only shared substantive helper; `get_demand` is the flow's own POST-to-GET/replay read and serializer. The factory, `PlanningRepository`, run/prediction models, idempotency/audit services, and JSON mapping move with the flow. `CoreBomAdapter` is the safe one-way collaborator and remains the sole BOM authority. `_forecast` should become a narrow injected completed-forecast reader (not duplicated and not generic `PlanningUtils`). **INGREDIENT_DEMAND_EXTRACTION_READY.** Future `generate/get` stay together because they share validation, persistence view, serialization, and replay continuity.

### Operational Procurement

- **Current lifecycle:** `DecisionPlanningService.generate_plans` validates unique strategies and persisted-inventory mode, uses `_forecast`, requires completed non-empty demand, registers JSON-hash idempotency, creates/links `ProcurementPlanRun(running)`, and **commits before compute**. It then calls `ProcurementPlanningService.build() -> InventorySimulationService`, persists plan/line rows, terminal completion, successful response status, `procurement_plans_generated`, and a second terminal commit. A compute `PlanningError` commits `blocked` and rethrows; an unexpected compute exception commits `failed` with `PROCUREMENT_PLAN_INFEASIBLE` and returns 500. Persistence failure after the initial commit leaves the already committed running run while uncommitted terminal work rolls back on close. `get_plans` independently validates forecast/store, resolves requested or latest run, maps sorted candidates/lines, and raises 500 for completed-without-plans.

| Stage | Write | Commit | Failure state |
|---|---|---:|---|
| Request/forecast/demand validation | none | no | precondition error; no run. |
| Lifecycle start | idempotency record + running run + resource link | yes | running row is externally visible before `build()`. |
| Domain compute failure | blocked status/code/message/completed time | yes | original error; no success audit. |
| Unexpected compute failure | failed status, normalized code/message/completed time | yes | normalized 500; no success audit. |
| Success | plans/lines, completed state, response status, audit | yes | post-initial persistence failure leaves committed running row. |

- **Idempotency/audit / ADR-014:** same-key/same-payload replay returns `get_plans` for the linked row, including blocked/failed rows; a changed body is `DUPLICATE_REQUEST` / `IDEMPOTENCY_KEY_REUSED` (409). The initial link has `response_status=None`; only success changes it to 200. Success audit is exactly `procurement_plans_generated`, resource type `procurement_plan_run`, source `planning_service`, and `{forecast_run_id, recommended_strategy}` in the terminal transaction. **Operational Procurement authority is unchanged:** `ProcurementPlanningService -> InventorySimulationService`; no operational route moves to core.
- **Dependencies / verdict:** `_forecast` is the same shared completed-forecast reader; `get_plans` is this lifecycle's read/replay/legacy collaboration. Factory, validation, repository/models, idempotency/audit, and response mapping move with the flow. `ProcurementPlanningService` remains a safe one-way computation collaborator. **OPERATIONAL_PROCUREMENT_EXTRACTION_READY.** A cohesive generate/get lifecycle is favored, and `get_plans` must be available one-way to legacy without returning through the facade.

- **R5 vs R6 / implementation sequence:** both flows are **R5_PREREQUISITE**, not `R6_BLOCKED_PREREQUISITE`: direct ORM, `session_factory`, `flush`, explicit commits, and current rollback behavior can move unchanged; no evidence requires a transaction/persistence redesign. The feasible acyclic target is `DecisionPlanningService facade -> {IngredientDemandLifecycle, OperationalProcurementLifecycle, LegacyPlanCompatibility}` and `LegacyPlanCompatibility -> {IngredientDemandLifecycle, OperationalProcurementLifecycle}`, with no reverse edge. Acceptance gates preserve public route/DTO/status/error behavior, store scope, idempotency, audit, persistence shape, commit ordering, failure semantics, authority, GET continuity, facade-only DPS, and no duplicated implementation. Rollback restores the facade body and removes the focused collaborator/wiring/tests/docs; no data conversion. Approved sequence: **R5.4 Ingredient Demand Application Boundary Extraction**, **R5.5 Operational Procurement Application Boundary Extraction**, **R5.6 retry LegacyPlanCompatibility extraction**. **NO ADR NEEDED** under ADR-015.
- **Test characterization / verification:** existing planning tests covered normal/blocked paths, typed DTO/OpenAPI, auth/store scope, and GET continuity. R5.3 adds demand completed/blocked replay, audit/resource-link, and unexpected-rollback checks; procurement success replay/conflict/audit, committed-running visibility and replay, blocked replay, and unexpected-failed persistence/replay. Focused planning characterization: `tests/test_planning_api.py` **10 passed, 1 warning**; targeted planning/idempotency/audit/compatibility/OpenAPI regression: **25 passed, 1 warning in 21.35s**. Production code was unchanged. Latest full-suite evidence remains **620 passed, 0 failed, 22 warnings** and is not claimed as R5.3 execution.

## R5.4 — Ingredient Demand Application Boundary Extraction — COMPLETE (2026-09-10)

- **Implementation:** moved the complete Ingredient Demand application lifecycle from `DecisionPlanningService` into `app/services/decision/ingredient_demand_lifecycle.py:IngredientDemandLifecycle`. Its `generate()` and `read()` methods own the existing completed-forecast validation, idempotency, `IngredientDemandRun`/prediction persistence, response mapping, completed/blocked behavior, audit, and POST-to-GET continuity. `DecisionPlanningService.generate_demand()` and `get_demand()` are now facade-only delegates.
- **Completed forecast collaboration:** `app/services/decision/completed_forecast_reader.py:CompletedForecastReader` now owns the exact shared store/forecast/completed/prediction lookup previously in `_forecast`. `DecisionPlanningService._forecast()` delegates to that reader for its retained flows, while `IngredientDemandLifecycle` depends on the reader directly. This removes duplication and creates no `IngredientDemandLifecycle -> DecisionPlanningService` edge.
- **Transaction / lifecycle preservation:** new Demand runs are still flushed, never committed, before `CoreBomAdapter`; no pre-computation running commit was introduced. Success still persists predictions, completed state, `ingredient_demand_generated`, and successful idempotency resource link in one terminal commit. `PlanningError` still commits `blocked` with its exact code/message and no success audit. Unexpected errors still roll back all uncommitted Demand/idempotency work; no failed row was introduced.
- **Authority / contract preservation:** the lifecycle delegates BOM unchanged to `CoreBomAdapter -> shelfcash_core`. Routes, `IngredientDemandRunResponse`, status/error envelope, `Idempotency-Key` behavior, auth, store scope, OpenAPI, ORM models, database schema, migrations, backfill, repository policy, and Unit of Work policy are unchanged. Successful replay remains linked to the demand run; keyed blocked replay remains forecast-scoped despite no normal success resource link. The body-less Demand POST has no distinct public payload with which to exercise a material same-key conflict; its current request-hash behavior is unchanged.
- **Evidence:** `tests/test_planning_api.py` (including facade delegation, success, blocked, unexpected rollback, replay, audit, GET continuity, store/auth, and OpenAPI) **11 passed, 1 warning in 11.64s**. BOM/forecast/OpenAPI regression: `tests/test_bom_adapter_regressions.py`, `tests/test_forecast_integration.py`, `tests/test_error_openapi_contract.py` **13 passed, 1 warning in 17.60s**. Full suite: **626 passed, 0 failed, 22 warnings in 263.02s**. `compileall -q app shelfcash_core shelfcash_forecast tests` and `git diff --check` passed. Runtime OpenAPI remains **58 paths / 70 operations**.
- **Rollback / next dependency:** rollback restores the former two Demand method bodies in the facade and `_forecast`, removes the two focused collaborators/wiring/tests/docs, and needs no data rollback. This is the first one-way prerequisite for legacy isolation; `LegacyPlanCompatibility` remains blocked by the still facade-owned Operational Procurement lifecycle (`generate_plans` and `get_plans`). **R5.5 — Operational Procurement Application Boundary Extraction** is recommended and not started. No new ADR is needed under ADR-015.

## R5.5 — Operational Procurement Application Boundary Extraction — COMPLETE (2026-09-10)

- **Implementation:** moved the complete Operational Procurement command/read lifecycle from `DecisionPlanningService` into `app/services/decision/operational_procurement_lifecycle.py:OperationalProcurementLifecycle`. Its `generate()` and `read()` methods own existing validation, idempotency, `ProcurementPlanRun`/plan/line persistence, completed/blocked/failed status mapping, audit, response composition, and replay/POST-to-GET continuity. `DecisionPlanningService.generate_plans()` and `get_plans()` are facade-only delegates.
- **Completed-forecast collaboration and authority:** `OperationalProcurementLifecycle` directly reuses the existing narrow `CompletedForecastReader`; it has no reverse dependency on `DecisionPlanningService` and no duplicated forecast validation. Operational computation remains exactly `ProcurementPlanningService -> InventorySimulationService` under ADR-014. No route uses `CoreProcurementAdapter` for this lifecycle, and Decision Procurement remains separate.
- **Transaction / failure preservation:** request validation remains before a run. The idempotency record, `ProcurementPlanRun(status=running)`, and resource link are still flushed and committed before `ProcurementPlanningService.build()`. Success still persists plans/lines, completed state, successful response status, and `procurement_plans_generated` in the terminal commit. A `PlanningError` still commits `blocked` with exact code/message and no success audit. An unexpected compute error still commits `failed` with `PROCUREMENT_PLAN_INFEASIBLE` and returns normalized 500 semantics; an unexpected final-persistence error still leaves the previously committed running row while terminal uncommitted work rolls back on session close.
- **Contract / persistence preservation:** successful, running, blocked, and failed replays retain current GET representations; changed request payload with the same key remains `DUPLICATE_REQUEST` / `IDEMPOTENCY_KEY_REUSED` (409). Store scope, auth, DTOs, status/error envelope, routes, and OpenAPI are unchanged. No ORM model, schema, migration, backfill, repository policy, or Unit of Work redesign was introduced.
- **Evidence / next dependency:** focused planning/facade lifecycle tests **12 passed, 1 warning in 14.07s**; BOM/forecast/OpenAPI regression **13 passed, 1 warning in 17.08s**; procurement/recipe/forecast regression **22 passed, 1 warning in 13.10s**; final full suite **627 passed, 0 failed, 22 warnings in 322.57s**. Final compile and whitespace gates passed. Both one-way canonical collaborators now exist, so `LegacyPlanCompatibility` can depend directly on `IngredientDemandLifecycle` and `OperationalProcurementLifecycle` without a reverse facade edge. Rollback restores the two former Operational Procurement method bodies in the facade, removes this collaborator/wiring/tests/docs, and needs no data rollback. **R5.6 — LegacyPlanCompatibility Extraction** is recommended and not started. No new ADR is needed under ADR-015.

## R5.6 — LegacyPlanCompatibility Extraction — COMPLETE (2026-09-10)

- **Implementation:** moved the public `/plan-runs` compatibility command and reads from `DecisionPlanningService` into `app/services/decision/legacy_plan_compatibility.py:LegacyPlanCompatibility`. `DecisionPlanningService.create_legacy_plan`, `get_legacy_plan_metadata`, and `get_legacy_plan_result` are facade-only delegates. The focused owner retains legacy strategy mapping, outer idempotency/replay, `PlanRunModel` lifecycle, Recommendation projection, `legacy_plan_generated`, terminal error mapping, metadata, and result composition.
- **One-way collaboration / authority:** the boundary directly uses `IngredientDemandLifecycle.generate()` and `OperationalProcurementLifecycle.generate()/read()`, never `DecisionPlanningService`. It injects `CompletedForecastReader` only for existing cutoff validation. Demand still delegates `CoreBomAdapter -> shelfcash_core`; Operational Procurement still delegates `ProcurementPlanningService -> InventorySimulationService` under ADR-014. Decision Procurement is not involved.
- **Transaction and compatibility preservation:** legacy idempotency plus `PlanRun(status=running)` still commit before nested work. Demand and Operational Procurement retain their independent current lifecycles. Final success still writes the canonical-run link, Recommendation rows, `legacy_plan_generated`, and completed PlanRun in one transaction. A nested `PlanningError` still commits legacy `blocked`; unexpected orchestration error still rolls back terminal projection then commits legacy `failed` with normalized `PROCUREMENT_PLAN_INFEASIBLE`. Replay returns current metadata, including blocked/current runs; changed same-key payload remains the existing 409 conflict. No failure audit was added.
- **Contract / persistence / downstream preservation:** routes, DTOs, status/error envelope, auth, store scope, idempotency, OpenAPI visibility/deprecation metadata, ORM models, schema, migrations, backfill, repositories, and Unit of Work policy are unchanged. `RecommendationModel` remains persisted compatibility data used by Purchase Order creation. Focused tests prove Recommendation/audit/completed finalization atomicity, terminal rollback/failed persistence, and legacy PlanRun-to-Purchase Order behavior.
- **Evidence / next review:** `tests/test_planning_api.py` **14 passed, 1 warning in 34.19s**; completion/forecast/BOM/error/OpenAPI regression (`tests/test_completion_behavior.py`, `tests/test_completion_response_contract.py`, `tests/test_forecast_integration.py`, `tests/test_error_openapi_contract.py`, `tests/test_bom_adapter_regressions.py`) **26 passed, 1 warning in 65.06s**; final full suite **629 passed, 0 failed, 22 warnings in 470.30s**. Final compile and whitespace gates passed. The R5.3 circular prerequisite is resolved. `/plan-runs` remains **CURRENT_COMPATIBILITY_REQUIRED**; its public removal remains **PUBLIC_API_DECISION_NEEDED**. **R5.7 — R5 Exit Review** is recommended and not started. No new ADR is needed.

## R5.7 — R5 Exit Review / Compatibility Handoff — COMPLETE (2026-09-10)

- **Exit decision: R5 COMPLETE.** R5 achieved its accepted objective: runtime compatibility was inventoried and classified, one proven-dead private helper was removed, active `/plan-runs` compatibility was isolated behind a focused owner, and retained public/runtime compatibility is intentional rather than accidental. No additional safe R5 implementation is required before phase close. R4 remains historically complete; R5 discovered and completed the one-way lifecycle prerequisites needed for legacy isolation.
- **Final ownership:** `DecisionPlanningService` remains the real owner of Decision Run generation/enrichment and What-if. It is a facade for `ReadDecisionPackage`, `BuildDecisionBrief`, `ExplainDecision`, `IngredientDemandLifecycle`, `OperationalProcurementLifecycle`, and `LegacyPlanCompatibility`. `LegacyPlanCompatibility` directly uses Demand and Operational Procurement collaborators with no reverse `LegacyPlanCompatibility -> DecisionPlanningService` edge. Demand remains `CoreBomAdapter -> shelfcash_core`; ADR-014 Operational Procurement remains `ProcurementPlanningService -> InventorySimulationService`; Decision Procurement remains `CoreProcurementAdapter -> shelfcash_core` and is not involved in legacy PlanRun work.
- **Compatibility inventory / retention:** `/plan-runs` is **CURRENT_COMPATIBILITY_REQUIRED / NOW ISOLATED**: public, OpenAPI-visible, non-deprecated, with external callers unknown. Store forecast-run compatibility is **CURRENT_COMPATIBILITY_REQUIRED** but already has explicit `ForecastService` ownership; no further R5 isolation is justified. `/forecasts` remains **DEPRECATED_BUT_ACTIVE**, verified with `deprecated=True` and active routes/tests. The ImportModel sync/read/backfill bridge remains **DEPRECATED_BUT_ACTIVE** because `ImportService` still writes it and falls back to it. Brief missing-assistant fallback remains **CURRENT_COMPATIBILITY_REQUIRED / OUT_OF_SCOPE** because it protects current read behavior. `LegacySupplierInventoryValueModel` remains **PROVEN_UNUSED_CANDIDATE**, safely deferred because it remains package-exported with migration/archive concerns; it is not proven dead. No additional R5 inventory item meets the full proven-dead removal gate.
- **Preservation and handoff:** R5 preserved Demand's no-precompute-running-commit/success/blocked/rollback semantics; Operational Procurement's committed running row and terminal completed/blocked/failed commits; and legacy PlanRun's initial commit, nested independent lifecycles, final Recommendation/audit/completed transaction, and finalization rollback-plus-failed persistence. Idempotency remains flow-specific and unchanged; success audits remain `ingredient_demand_generated`, `procurement_plans_generated`, and `legacy_plan_generated`. `RecommendationModel` remains active Purchase Order input. No route/DTO/status/error/auth/store-scope/deprecation/OpenAPI drift or ORM/schema/migration/backfill/repository/Unit-of-Work redesign occurred. Final full suite: **629 passed, 0 failed, 22 warnings in 336.89s**; final compile and whitespace gates passed.
- **Future boundary:** public compatibility removal remains **PUBLIC_API_DECISION_NEEDED**: no in-repository caller is not proof that external callers do not exist, and ADR-007 requires an explicit public-contract decision. R6 receives only persistence architecture concerns: mixed repository/direct ORM, distributed `session_factory`, direct ORM transaction ownership/commit policy, and persisted JSON/versioning policy. Do not start R6 implementation without its entry review. **NO NEW ADR NEEDED** for R5 closure.

## R6 — Entry Review: Persistence & Transaction Architecture Characterization — COMPLETE (2026-09-10)

- **Entry decision:** R6 Entry Review is complete; no production code, schema, repository, or Unit of Work (UoW) change was made. The review distinguishes accepted fact from proposal: ADR-015 preserves the focused application owners created in R4/R5, and no accepted ADR requires repository-everywhere or a global UoW. Direct SQLAlchemy ORM remains an acceptable current pattern where the application owner opens the session and explicitly commits/rolls back.
- **Ownership map:** `IngredientDemandLifecycle`, `OperationalProcurementLifecycle`, `LegacyPlanCompatibility`, `ForecastService`, `ImportService`, `DecisionPlanningService` (Decision Run only), `CompletionService`, catalog/menu/recipe services each receive a session factory and own their command session(s). `AuditService`/`AuditLogRepository` and `IdempotencyService`/`IdempotencyRepository` only add/flush to the caller-owned session; they do not commit. `ImportBusinessPersistenceService` receives its caller's session and flushes only. `ProcurementPlanningService` receives the Operational Procurement session for computation and does not own a transaction. Read collaborators (`ReadDecisionPackage`, `BuildDecisionBrief`, `OperationalService`, catalog/menu/recipe reads) open short read sessions with no write transaction abstraction required.
- **Frozen transaction facts:** Ingredient Demand flushes a new running row but never commits it before `CoreBomAdapter`; success commits predictions/completed/audit/resource link, `PlanningError` commits blocked, and unexpected work rolls back. Operational Procurement commits idempotency plus `ProcurementPlanRun(running)` before `ProcurementPlanningService.build()`, then commits terminal completed/blocked/failed work separately. Legacy PlanRun commits outer idempotency plus running state before nested Demand/Procurement lifecycles, then commits its Recommendation/link/audit/completed group; finalization failure rolls that group back before failed-state persistence. Decision Run commits deterministic `DecisionRunModel.package_json` before separately committing optional assistant enrichment; What-if is in-memory only. Forecast training/inference intentionally persist running/model status before external core/artifact work and commit terminal success/failure separately. Completion/Purchase Order operations use one explicit command session whose audit/idempotency rows join the final write commit; duplicate-key integrity handling rolls that command back.
- **Concrete R6 problem / hotspot:** `ImportService.process()` sets `ImportJob.status = processing` and writes its start audit with `flush()` only, then performs pipeline parsing and `ImportBusinessPersistenceService` business writes in that same uncommitted transaction. The existing `status == processing` guard is therefore not visible to an independent session during the expensive work, so it cannot reliably claim a processing job across concurrent callers. This is concrete transaction-ownership/concurrency risk, unlike the intentionally different planning commit sequences. Import has additional intentional checkpoints for atomic validation/preview and terminal normalized-job + legacy-bridge + audit persistence. Its result file is a best-effort derivative: normalized `result_json` is authoritative and tested to serve API results after the file is absent.
- **Repository / ORM findings:** `NormalizedImportRepository`, `CatalogRepository`/`InventoryRepository`/`HistoryRepository`, `RecipeRepository`, `StoreRepository`, `PlanningRepository`, and forecast repositories provide aggregate/query value while leaving commit ownership to application services. `ForecastDataRepository` and `InventoryConstraintRepository` are query helpers. `AuditLogRepository` and `IdempotencyRepository` are thin persistence helpers deliberately joined to the caller's transaction. `SQLiteImportRepository` owns its own sessions/commits but has no production caller; it remains a migration-foundation test fixture rather than an active runtime boundary. Direct ORM in focused lifecycle, Completion, catalog/menu, and computation collaborators is currently explicit and not a style-only defect.
- **Persisted JSON / filesystem findings:** Decision Run owns `request_json`, `package_json`, and `warnings_json`; `ReadDecisionPackage`/Brief/Explanation consume `package_json`, and assistant enrichment updates it only after deterministic commit. Planning and forecast JSON fields are produced/consumed by their lifecycle owners. Import normalized rows hold current profiles, mappings, validation, result, and write summaries; the active ImportModel bridge remains deprecated-but-active. These blobs have no explicit schema-version field, but ADR-013 means old SQLite rows alone do not justify migration/backfill machinery. No concrete same-version multi-shape failure was found; explicit versioning is **NEEDS_FURTHER_CHARACTERIZATION**, not R6.1. Forecast artifacts and Import result files have intentionally non-atomic database/file lifecycles and are not business truth replacements.
- **R6 direction / next slice:** keep direct ORM in focused services, use repositories selectively where they already convey query/aggregate meaning, and make transaction claims explicit only where evidence warrants it. A global UoW and repository-everywhere are **unproven and rejected as R6 defaults**; no ADR is needed for a narrow behavior-preserving Import slice. **R6.1 — Import Processing Claim Transaction Ownership Hardening** is next and is not started: freeze/characterize concurrent `process()` behavior, make the processing claim persist before expensive work only if tests prove the existing guard is ineffective, preserve validation/preview/terminal/legacy-bridge/audit/file semantics and API errors, and keep rollback code-only. No route, DTO, business persistence algorithm, filesystem redesign, schema/migration, generic repository, or UoW work belongs to that slice. Later R6 work is provisional: reassess Import terminal finalization after R6.1; then assess a single Completion write family and Decision Package JSON contract only if new evidence warrants it; finish with an R6 exit review.
- **Boundary and contract preservation:** Forecast remains `shelfcash_core`; Ingredient Demand remains `CoreBomAdapter -> shelfcash_core`; Operational Procurement remains `ProcurementPlanningService -> InventorySimulationService`; Decision Procurement remains `CoreProcurementAdapter -> shelfcash_core`. R5 compatibility remains isolated and public removal is still `PUBLIC_API_DECISION_NEEDED`. No route/DTO/status/error/auth/store-scope/deprecation drift, no model/schema/migration/backfill, and no narrative authority change occurred.
- **Verification:** R6 targeted stabilization (`tests/test_import_normalized.py`, `tests/test_import_business_persistence.py`, `tests/test_forecast_api.py`, `tests/test_forecast_integration.py`, `tests/test_planning_api.py`, `tests/test_completion_behavior.py`, `tests/test_completion_response_contract.py`, `tests/test_error_openapi_contract.py`) passed **94 tests, 1 warning in 77.62s**. Full suite passed **629 tests, 0 failed, 22 warnings in 441.53s**. `compileall -q app shelfcash_core shelfcash_forecast tests` and `git diff --check` passed. Existing OpenAPI contract tests retain **58 paths / 70 operations**.

## R6.1 — Import Processing Claim Transaction Ownership Hardening — COMPLETE (2026-09-11)

- **Characterization and defect:** two independent SQLite/SQLAlchemy sessions can both read `confirmed`. After session A only flushes `processing`, session B still reads `confirmed` and its competing write fails with `database is locked`; after A commits a plain status update, B's stale ORM instance can still write `processing`. The old lifecycle therefore relied on a long-held SQLite writer lock and had no durable business-level exclusive claim.
- **Implementation:** `ImportService._acquire_processing_claim()` now performs a narrow conditional `UPDATE import_jobs ... WHERE import_id = ? AND status IN ('confirmed', 'completed')`, checks the affected-row count, and commits only the winning `processing` transition before expensive work. A zero-row claimant rolls back and re-evaluates fresh state; a current `processing` row preserves `IMPORT_PROCESSING` / 409. The existing `import_processing_started` audit remains in the worker transaction, while normalized business persistence, active ImportModel bridge, `import_completed`, terminal status, and result JSON retain their existing terminal grouping.
- **Failure and compatibility preservation:** a post-claim pipeline exception still rolls back worker work and invokes the existing `_mark_failed()` lifecycle, producing `failed`, processing issue/error, legacy bridge sync, and `import_failed`; failed jobs remain non-retryable through the existing `INVALID_STATE_TRANSITION` contract. Preview/atomic-validation behavior, result-file best-effort behavior, and `result_json` as API truth are unchanged. No model/schema/migration/backfill, repository redesign, Unit of Work, route/DTO/error/auth/store-scope/OpenAPI, or business mapping change occurred.
- **Concurrency proof:** the deterministic barrier test holds the winning pipeline after its durable claim. The loser receives `IMPORT_PROCESSING` before pipeline execution; pipeline and `ImportBusinessPersistenceService.persist()` each run exactly once, and only one `import_processing_started` plus one `import_completed` audit event are persisted. The legacy ImportModel row remains the single current bridge record.
- **Verification:** import lifecycle/legacy bridge/business persistence tests passed **59 tests, 1 warning in 92.66s**; API/OpenAPI/planning/forecast/completion regression passed **40 tests, 1 warning in 103.06s**. Required full suite passed **633 tests, 0 failed, 22 warnings in 537.58s**. `compileall -q app shelfcash_core shelfcash_forecast tests` and `git diff --check` passed; OpenAPI contract tests retain **58 paths / 70 operations**.

## R6.2 — Completion Purchase Order Transaction Characterization — COMPLETE (2026-09-11)

- **Current owner and contract:** all six current Purchase Order operations remain API-key protected and store-scoped: create/list/get/patch/confirm/receive. `CompletionService.po()` owns the command session and direct SQLAlchemy ORM access; routes and DTOs remain unchanged. `RecommendationModel` remains active upstream compatibility data: create reads a completed `PlanRunModel` plus store-scoped recommendations, groups them by supplier, and persists draft Purchase Orders and lines without recalculating procurement truth.
- **Transaction map:** create flushes a possible budget row, Purchase Orders and lines for response construction, then writes `purchase_order_create` audit and optional idempotency response before one final commit. Patch and confirm load the scoped PO/lines, validate their current state and body version, mutate their lines/budget/PO state, write audit, and commit once. Receive validates `ordered`/`partially_received`, writes lots, receipt movements, `PurchaseReceiptModel` rows, line received quantities, budget reservation/spend, PO terminal/partial state, expiry classification, audit, and optional idempotency response in one final commit. `AuditService`/`AuditLogRepository` and `IdempotencyService`/`IdempotencyRepository` only add/flush in that caller session; `IngredientExpiryClassificationService` receives the same session. No PO collaborator opens an independent session or performs a hidden commit/rollback.
- **Atomicity and failure evidence:** all ordinary PO operation writes are pending until that final commit; an `IntegrityError` explicitly rolls back and maps existing receipt/lot identity conflicts, while other exceptions leave the context-managed session to discard uncommitted work on close. Thus a mid-command line/receipt/lot/movement/audit failure cannot leave a partially committed final group. This is not a reason for Repository or Unit of Work (UoW) redesign.
- **Concrete concurrency defect:** **`CONCURRENT_RECEIVE_RISK` and `PO_STATUS_INVENTORY_DIVERGENCE` are confirmed.** A deterministic two-independent-session barrier held two receive calls after both had read PO version `2` and remaining quantity `10`. With distinct delivery and lot identities, both calls returned `received`; two receipt rows, `+20` inventory movement, and two `purchase_order_receive` audits committed, while the stale second write left the PO line at received quantity `10` and budget spent `1000`. The Python body-version check is not a database compare-and-set, so it does not reject the second stale writer. This is a current-version correctness defect, not merely a style concern.
- **Other concurrency / idempotency facts:** create and receive have optional endpoint-local idempotency; same-key replay returns its stored response and different payload remains the current 409 duplicate-request contract. Confirm and patch have body version guards but no durable SQL conditional update. Create does not consume recommendations, so current code permits more than one draft PO from a recommendation when callers use distinct/no idempotency keys; no one-time recommendation contract was found. Confirm has no receipt/inventory side effect; receive is the proven high-impact race. Current PO states are `draft -> ordered -> partially_received -> received`; patch is draft-only, confirm is draft-only, and receive requires ordered/partially received.
- **R6 direction / next slice:** global UoW and repository-everywhere remain **NOT JUSTIFIED**. **R6.3 — Purchase Order Receive Optimistic-Concurrency Hardening** is recommended and NOT STARTED. It must make the existing PO version/state check a database-enforced compare-and-set before any receipt/inventory side effect, ensure a losing concurrent caller rolls back before writes, and preserve the one final success transaction, audit/idempotency/error/store/API/OpenAPI behavior. No schema, migration, business/procurement/FEFO authority, Recommendation mapping, repository-wide refactor, or UoW belongs to that slice.
- **Verification:** focused PO behavior plus deterministic concurrency characterization passed **2 tests, 1 warning in 2.94s**; Completion/PO/planning/error/OpenAPI regression passed **34 tests, 1 warning in 76.34s**. Required full suite passed **634 tests, 0 failed, 22 warnings in 520.40s**. Final compile and whitespace gates passed; OpenAPI remains **58 paths / 70 operations**. R6.2 changes no production code.

## R6.3 — Purchase Order Receive Optimistic-Concurrency Hardening — COMPLETE (2026-09-11)

- **Defect fixed:** before R6.3, two independent receive sessions could both pass the stale Python `body.version == po.version` check and commit additive receipt/inventory/audit work. `CompletionService._claim_receive_version()` now performs a SQLite-compatible `UPDATE purchase_orders` guarded by `po_id`, `store_id`, expected `version`, and existing eligible statuses (`ordered`, `partially_received`). It increments `version` only when exactly one row matches, refreshes the loaded ORM object, and the normal receive path does not increment it a second time.
- **Loser and contention behavior:** a zero-row CAS result rolls back the caller's pending work, re-reads the scoped PO, and preserves current not-found, `VERSION_CONFLICT`, or invalid-state mapping. The deterministic SQLite two-session barrier observed one writer wait for the other transaction; after the winner commits, the loser receives `VERSION_CONFLICT` / 409 rather than a raw SQLite lock error. The loser writes no lot, receipt, movement, budget mutation, PO-line change, idempotency success response, or `purchase_order_receive` audit.
- **Atomicity preserved:** CAS is an uncommitted first write in the existing one final receive transaction; there is no durable receive-claim commit. The winner still atomically writes lot/movement/receipt, line quantities, budget, PO terminal/partial status, expiry classification, audit and optional idempotency response before the one commit. Injected post-CAS audit failure rolls back the CAS version bump and all receive side effects: the PO remains ordered at version 2, received quantity remains zero, budget reservation remains intact, and no receipt/movement/audit is added.
- **Regression / boundaries:** one successful receive retains exactly one version progression; existing partial then reloaded sequential receive coverage remains green. Receipt/lot `IntegrityError` mapping, optional idempotency replay/conflict, Recommendation-to-PO compatibility, store/auth/API/OpenAPI, procurement and FEFO authority remain unchanged. No model/schema/migration/backfill, repository redesign, Unit of Work, database replacement, or public-contract change occurred. **NO NEW ADR NEEDED.**
- **Verification:** focused PO state-machine/concurrency/rollback test set passed **2 tests, 1 warning in 2.30s**; broader Completion/PO/planning/error/OpenAPI regression passed **34 tests, 1 warning in 39.70s**. Required full suite passed **634 tests, 0 failed, 22 warnings in 266.24s**. Final compile and whitespace gates passed; OpenAPI remains **58 paths / 70 operations**. **R6.4 — Decision Run Persistence/Enrichment Transaction Characterization** is recommended and NOT STARTED.

## R6.4 — Decision Run Persistence / Enrichment Transaction Characterization — COMPLETE (2026-09-11)

- **Intentional two-phase boundary:** `DecisionPlanningService.generate_decision()` refreshes current Demand, computes the deterministic Decision Procurement package through `CoreProcurementAdapter -> shelfcash_core`, then persists one `DecisionRunModel` with request/package/warnings JSON, deterministic status, scenario metadata, selected strategy and timestamps in its own session/commit. Only after that commit does `_generate_and_persist_overall_summary()` open a separate session, build facts from the committed package, run guarded Overall Summary and Ingredient Synthesis wording/fallback work, write only `package_json.assistant` (`overall_summary`, `ingredient_synthesis`, diagnostics), and commit. There is no Decision Run audit event in either transaction.
- **Failure/read isolation:** provider, timeout, schema, and grounding failures return validated deterministic fallbacks which are persisted by the assistant transaction. A forced assistant `commit()` failure rolls back that package update; the pre-existing deterministic package remains readable through `ReadDecisionPackage`, and `BuildDecisionBrief` reconstructs deterministic Summary/Synthesis without a provider call or GET-time backfill. Thus crash or failure between commits leaves a valid current compatibility state rather than invalidating business truth. `ExplainDecision` and What-if are read-time/hypothetical operations and do not persist a Decision Run mutation.
- **Transaction ownership / long-call finding:** the deterministic command session owns its first commit; the assistant helper owns its second commit. The assistant helper opens a SQLAlchemy session and performs reads before possible Qwen/OpenRouter calls, but makes no write before those calls; its only write is the later dirty `package_json` assignment. Therefore no SQLite **write** transaction is held across Qwen latency. The intended replay branch returns the persisted package without invoking enrichment; independent of the idempotency defect below, no current competing public assistant writer or business-field overwrite path was found. Direct ORM remains an explicit acceptable pattern; neither Repository consolidation nor a Unit of Work is justified.
- **Concrete defect found — `DECISION_RUN_IDEMPOTENCY_TRANSACTION_GAP`:** when a key is supplied, the first validation session calls `IdempotencyService.register()`, which flushes but does not commit. That session closes before Demand refresh and deterministic persistence. Its record is therefore rolled back and is absent from the later deterministic session that attempts to link it to the new `DecisionRunModel`. This is a current transaction-continuity/idempotency correctness defect, independent of the intentional assistant split. Same-key Decision Run replay cannot be considered preserved until this gap is fixed.
- **R6.5 recommendation:** **Decision Run Idempotency Transaction Continuity Hardening** must make register/replay/resource-link ownership continuous with the deterministic DecisionRun transaction while preserving current request/response/conflict semantics, Demand/Procurement authority, deterministic-before-assistant commit, assistant fallback/read behavior, API/OpenAPI, schema, and no-UoW/direct-ORM policy. It must not merge enrichment with deterministic persistence. **NO NEW ADR NEEDED.**
- **Verification:** Decision/Brief/Explanation/Overall Summary/What-if and OpenAPI regression passed **63 tests, 1 warning in 61.27s**. New focused assistant-commit rollback/read-fallback and idempotency-session-gap tests passed **2 tests, 1 warning in 4.00s**. Required full suite passed **636 tests, 0 failed, 22 warnings in 491.03s**. `compileall -q app shelfcash_core shelfcash_forecast tests` and `git diff --check` passed; generated OpenAPI remains **58 paths / 70 operations**.

## R6.5 — Decision Run Idempotency Transaction Continuity Hardening — COMPLETE (2026-09-11)

- **Defect closed / selected boundary:** before this slice, a new Decision Run key was registered and only flushed in the validation session. That session closed before Demand/core work and rolled the record back, so the later `DecisionRunModel` transaction could not reliably link it. The validation session now performs a read-only lookup for an existing key. For a new key, the existing deterministic persistence session registers the record, adds the `DecisionRunModel`, sets the `decision_run` resource link and HTTP 200 metadata, then commits that whole deterministic group together. Shared `IdempotencyService` remains caller-transaction-owned and unchanged.
- **Replay, conflict, race, and failures:** an existing same-hash key returns its linked Decision Run before computation; a different request hash retains the existing `IDEMPOTENCY_KEY_REUSED` conflict. A local unique-record `IntegrityError` during concurrent final persistence rolls back and re-resolves the fresh record, yielding replay/conflict rather than a raw database error. A two-thread/two-session barrier test proves one persisted Decision Run, one durable record/link, and the same resource returned to both same-key callers. Core deterministic failure creates no key record; injected deterministic commit failure rolls back both the new record and Decision Run. Assistant failure occurs only after the deterministic group commits, so its rollback leaves the durable key/link and deterministic package valid.
- **Boundary preservation:** expensive Demand/core computation runs before the final write transaction; no SQLite write transaction spans that computation or Qwen/OpenRouter latency. The R6.4 deterministic-commit then optional assistant-commit split, missing-assistant Brief fallback, current JSON shapes, public idempotency header semantics, and Demand/Operational Procurement/Legacy PlanRun idempotency lifecycles remain unchanged. No audit was added, and no schema/migration/backfill, Repository redesign, Unit of Work, API/OpenAPI, business authority, or narrative architecture change occurred. **NO NEW ADR NEEDED.**
- **Verification / next review:** keyed success/replay/conflict and post-assistant-failure continuity are covered in Decision API tests; deterministic failure, final persistence rollback, and two-session same-key persistence are covered in focused Decision intelligence tests. Targeted Decision/Brief/Explanation/Overall Summary/What-if/planning/error/OpenAPI regression passed **85 tests, 1 warning in 39.70s**; full suite passed **638 tests, 0 failed, 22 warnings in 199.94s**. `compileall`, `git diff --check`, and generated OpenAPI (**58 paths / 70 operations**) passed. The next evidence-backed review is **R6.6 — Import Terminal Finalization Transaction Characterization** (NOT STARTED): reassess the remaining Import normalized-persistence, legacy-bridge, audit, terminal-state, and best-effort artifact grouping after R6.1's claim hardening; do not assume a defect or implement a redesign.

## R6.6 — Import Terminal Finalization Transaction Characterization — COMPLETE (2026-09-11)

- **Terminal success and failure ownership:** after R6.1's durable CAS processing claim, `ImportService` owns the processing session and final commit. `ImportBusinessPersistenceService` uses that caller session and only flushes: it writes the accepted normalized business families (sales/usage, inventory/receipts, catalog/menu/recipe, supplier terms, settings/constraints/calendar as present in confirmed sheets), expiry classification and related issues. The same terminal transaction then writes `result_json`, validation/business-write summaries, schema/persisted timestamps, completed ImportJob state, the active `ImportModel` legacy bridge, and `import_completed`; one commit makes that group visible. Forced post-flush persistence, bridge, and success-audit failures each roll that group back, then `_mark_failed()` opens a new session to write failed status/error issue, failed legacy projection, and `import_failed` before its own commit.
- **Compatibility and artifact findings:** the legacy bridge remains **DEPRECATED_BUT_ACTIVE** and is atomic with canonical terminal persistence. `result_json` is the current result API source of truth; the JSON artifact file is written only after the database commit and its `OSError` is swallowed after temporary-file cleanup. A forced file failure leaves completed normalized data, bridge, result JSON and `import_completed` intact; GET result succeeds without the file. Conversely, terminal DB failure cannot produce this result artifact because the artifact write is post-commit. Handled processing failures become failed/non-retryable rather than remaining processing. A process crash or a failure inside `_mark_failed()` itself can still leave the already durable processing claim; no stale-claim recovery exists, but this is the separate catastrophic termination/finalization-failure residual, not the demonstrated normal handled path.
- **Concrete defect — `IMPORT_PROCESSING_STARTED_AUDIT_LONG_WRITE_TRANSACTION`:** `import_processing_started` is written in the post-claim processing session before `pipeline.process_sheet()`. `AuditLogRepository.add()` immediately flushes, so SQLite obtains a write transaction before canonical normalization/validation and holds it until terminal commit or rollback. A deterministic independent-session probe, with a 1ms SQLite busy timeout while pipeline work is entered, receives `database is locked`. This is a current SQLite contention/correctness-availability risk and means R6.1's durable claim does not by itself keep the long processing path write-free. It is not a Repository or Unit of Work justification.
- **R6.7 recommendation:** **Import Processing-Started Audit Transaction Scope Hardening** (NOT STARTED) must remove this audit-induced long write scope while preserving exactly-one processing-started audit, existing handled-failure audit semantics, R6.1 claim exclusivity, terminal atomicity, legacy bridge, result JSON/file behavior and public contract. It must not change Import state/API/schema, add a generic retry/UoW, or redesign filesystem persistence. **NO NEW ADR NEEDED.**
- **Verification:** terminal persistence/bridge/audit/file-failure and current claim/failure tests plus Import/Forecast/Planning/Completion/error/OpenAPI regression passed **103 tests, 1 warning in 82.78s**. Full suite passed **643 tests, 0 failed, 22 warnings in 222.92s**; `compileall` and generated OpenAPI (**58 paths / 70 operations**) passed.

## R6.7 — Import Processing-Started Audit Transaction Scope Hardening — COMPLETE (2026-09-11)

- **Defect fixed / local design:** before R6.7, `ImportService.process()` added `import_processing_started` to the long processing/terminal session. The unchanged `AuditLogRepository.add()` correctly flushed that record, but acquired SQLite's write lock before `process_sheet()` normalization/validation. `ImportService._record_processing_started()` now opens a short caller-owned session only after R6.1's durable claim, validates the claimed state, records the unchanged audit payload, and commits before the long pipeline begins. The shared audit helper remains unchanged; there is no hidden helper commit.
- **Exact transaction shape and behavior:** success is claim commit → start-audit commit → pipeline → unchanged terminal group (canonical normalized writes, result JSON/status, legacy bridge, `import_completed`) commit → best-effort post-commit artifact. A handled post-start failure is claim commit → start-audit commit → terminal rollback → unchanged `_mark_failed()` failed-state/legacy/`import_failed` commit. A start-audit persistence failure after a successful claim skips the pipeline and enters `_mark_failed()`; an invalid pre-claim state still raises its existing `INVALID_STATE_TRANSITION` without being failed. Thus the start audit is now durable historical evidence for a real winner and precedes terminal success/failure; concurrency losers still write no start audit.
- **Lock and boundary proof:** the R6.6 deterministic probe now sees the start-audit `INSERT` before `process_sheet()` but an independent SQLite writer (1ms busy timeout) flushes and commits successfully during the pipeline window. No claim, terminal business write, legacy bridge, result JSON/file, API, schema, state machine, or mapping/business behavior changed. R6.6 terminal bridge/audit/persistence rollback tests remain green. The separate catastrophic residual remains out of scope: a process crash or database failure inside `_mark_failed()` can still leave a durable processing state.
- **R6 assessment / next:** no additional concrete persistence defect was found after R6.1, R6.3, R6.5, and R6.7. Direct ORM remains acceptable at these explicit boundaries; Repository consolidation and Unit of Work remain unjustified. **R6 Exit Review** is recommended and NOT STARTED. **NO NEW ADR NEEDED.**
- **Verification:** focused claim/lock/audit/failure/terminal-artifact regressions plus Import/Forecast/Planning/Completion/error/OpenAPI stabilization passed **104 tests, 1 warning in 87.09s**. Full suite passed **644 tests, 0 failed, 22 warnings in 235.90s**; `compileall` and generated OpenAPI (**58 paths / 70 operations**) passed.

## R6 Exit Review — Persistence & Transaction Architecture Closure — COMPLETE (2026-09-11)

- **Closure decision:** **R6 COMPLETE.** The review rechecked the R6 regression suite and the current code's transaction owners. No concrete current-version persistence, rollback, concurrency, idempotency, public-contract, schema, or business-authority blocker remains. R6 met its objective without a global persistence rewrite.
- **Defects closed:** R6.1 made the Import processing claim an explicit committed SQLite compare-and-set; R6.3 moved Purchase Order receive ownership to a database version/state guard inside its existing atomic receive transaction; R6.5 made Decision Run idempotency registration/resource linking atomic with deterministic DecisionRun persistence; R6.7 moved only Import's processing-start audit to its own short caller-owned transaction, removing its pipeline-long SQLite write lock. Regression coverage retains the original two-session/race and failure probes.
- **Characterized boundaries retained:** Purchase Order create/confirm/receive remain caller-owned multi-aggregate transactions, with the receive guard rolling back with later receive work. DecisionRun deterministic persistence commits before optional assistant enrichment; the assistant writes only `package_json.assistant` and may fail without invalidating business truth or idempotent replay. Import terminal canonical persistence, active legacy `ImportModel` bridge, `result_json`, completed state and completion audit remain one terminal transaction; result artifacts remain post-commit best effort.
- **Architecture conclusion:** direct SQLAlchemy ORM remains acceptable where a focused application owner owns its session and commit. Shared Audit and Idempotency helpers still add/flush in the caller session and do not self-commit. Selective repositories remain useful; repository-everywhere and a global Unit of Work remain **NOT JUSTIFIED**. Different lifecycle commit shapes are intentional, including Ingredient Demand's no-precompute-running commit, Operational Procurement's running-plan commit, Legacy PlanRun's staged compatibility commits, Decision Run's fault-isolation commit, and Import's claim/start-audit/terminal boundaries.
- **Residual classification:** a process crash after an Import claim, or a database failure inside `_mark_failed()`, can leave an ImportJob in durable `processing`; this is a **RESIDUAL_OPERATIONAL_RECOVERY_RISK**, not a demonstrated handled-request correctness defect and is deferred rather than converted into a watchdog/lease redesign. SQLite remains single-writer by design; R6 removed the proven unnecessary Import audit lock but does not promise unlimited write concurrency. No same-version persisted-JSON defect or migration/backfill need is evidenced under ADR-013.
- **Compatibility / authority:** R6 preserved `/plan-runs`, forecast compatibility routes, deprecated-but-active `/forecasts`, the active Import legacy bridge, Brief missing-assistant fallback, and `RecommendationModel → PurchaseOrder` creation. Forecast, Ingredient Demand, Operational Procurement, and Decision Procurement retain their accepted deterministic authorities; Qwen remains a bounded language/ambiguity integration. Overall Summary remains HYBRID.
- **Verification:** targeted R6 Import/PO/Decision/Planning regressions passed **74 tests, 1 warning in 72.69s**; the exit-review full suite passed **644 tests, 0 failed, 22 warnings in 251.35s**. Runtime OpenAPI remains **58 paths / 70 operations**; `compileall` and `git diff --check` passed. Exit review adds no production code, schema, migration, API, or ADR change.

## R7 Entry Review — Stabilization — COMPLETE (2026-09-11)

- **Stabilization baseline:** the current post-R1–R6 runtime is broadly stable: the Import claim/lock/terminal regressions, PO receive winner-only regression, DecisionRun same-key/idempotency and assistant-failure isolation, Forecast shadow isolation, planning/legacy compatibility, provider fallback, error/OpenAPI, health, and clean-database migration/startup checks are green. The broad targeted stabilization group passed **194 tests, 5 warnings in 178.76s**; the full suite passed **645 tests, 0 failed, 22 warnings in 251.27s**. Runtime OpenAPI remains **58 paths / 70 operations**; `compileall` and `git diff --check` passed.
- **Integration / authority findings:** Import → normalized data, Forecast → Demand, Demand → Operational Procurement, Decision Run → Brief/Explanation/What-if, Recommendation → PurchaseOrder, and PO Receive → inventory retain their tested boundaries. Forecast production remains `shelfcash_core`; Operational Procurement remains `ProcurementPlanningService -> InventorySimulationService`; Decision Procurement remains `CoreProcurementAdapter -> shelfcash_core`. Shadow forecast stays non-authoritative. Qwen remains language/ambiguity-only; deterministic Brief defaults, HYBRID Overall Summary, provider fallbacks, and What-if non-mutation remain intact. Current API key, store-scope, typed error-envelope, OpenAPI, and R5 compatibility behavior remain green.
- **Concrete stabilization finding — `PO_CONFIRM_OPTIMISTIC_CONCURRENCY_GAP`:** R7 adds a deterministic two-session/service characterization at the existing `CompletionService._budget()` boundary. Two callers both read one draft PO at version `1`, synchronize, and both call `po("confirm", ..., POConfirmIn(version=1, ...))`. The current Python-only version/status check permits **two successes** and writes **two `purchase_order_confirm` audits**. The durable scalar rows mask the defect (`status=ordered`, `version=2`, `reserved_budget=1000`) because the stale second write overwrites equal scalar values rather than producing a second version/reservation. The probe passed identically three standalone runs. This violates the existing expected-version transition contract and makes audit history / command outcome non-authoritative; it is distinct from the R6.3 receive race.
- **Risk classification:** `PO_CONFIRM_OPTIMISTIC_CONCURRENCY_GAP` is **HIGH**, reproducible, current-version and store-scoped, with no API/schema/authority change required for remediation. Unraced draft PATCH and other Completion writes remain **OBSERVATION_ONLY / no current defect evidence**. Mixed direct ORM/repository use, session-factory scatter, permissive current JSON reads, legacy compatibility surfaces, core/shadow overlap, service breadth, and the 22 warnings remain non-blocking debt/known dependency warnings. Warning inventory is one Starlette/httpx TestClient deprecation plus 21 Python sqlite datetime-adapter deprecations in migration/inventory tests; no unclosed-resource warning or raw error leak was observed.
- **R7.1 recommendation — Purchase Order Confirm Optimistic-Concurrency Hardening (NOT STARTED):** keep the current draft → ordered state machine, request/response DTO and version/error contract. Move confirm authorization from the stale Python object to a SQLite-compatible database compare-and-set matching `po_id`, `store_id`, `version=expected_version`, and `status=draft`, with its one version progression in the existing confirmation transaction before budget reservation/audit. The winner alone may reserve budget, mark ordered, and write `purchase_order_confirm`; a zero-row result must re-read and preserve the existing not-found/version/state behavior. Acceptance: the new race becomes one success/one `VERSION_CONFLICT`, one reservation, one audit, final version `N+1`, sequential confirm behavior and failure rollback remain unchanged, and R6.3 receive stays untouched. Rollback is limited to this narrow confirmation guard, its focused test/docs; no schema/migration/API/Repository/UoW/CompletionService decomposition or business-rule change.

## R7.1 — Purchase Order Confirm Optimistic-Concurrency Hardening — COMPLETE (2026-09-11)

- **Defect closed:** Confirm previously authorized `draft` and request `version` only on a loaded ORM object, so two stale version-`V` callers could both report success and write two `purchase_order_confirm` audits. `CompletionService._claim_confirm_version()` now performs one SQLite conditional update matching `po_id`, `store_id`, `version=expected_version`, and `status=draft`, advancing the version exactly once. The caller-owned Confirm transaction refreshes the winning ORM row, then performs the unchanged budget reservation, `ordered` transition, `confirmed_at`, response projection, audit, and final commit. The former Python version increment was removed to prevent a double increment.
- **Winner, loser, and rollback behavior:** a zero-row CAS rolls back and reloads the scoped PO before retaining the existing precedence: not found, stale `VERSION_CONFLICT`, then invalid state. The deterministic two-session regression now produces **one success**, **one `VERSION_CONFLICT`**, one `ordered` PO at `V+1`, one budget reservation, and one success audit; it passed three consecutive focused runs. Sequential stale and current-version non-draft API requests retain `VERSION_CONFLICT` and `INVALID_STATE_TRANSITION` respectively. Injected post-CAS budget and success-audit failures leave the PO `draft` at its original version, reservation unchanged, and no success audit, proving the guard is not a durable pre-confirm claim.
- **Scope and verification:** Confirm remains one atomic caller-owned transaction; Receive's R6.3 guard is unchanged and Confirm → Receive remains green. No schema/migration/backfill, API/DTO/OpenAPI/auth/store-scope, state-machine, budget formula, procurement authority, shared audit helper, Repository, Unit of Work, or service redesign changed. Focused Completion plus R7 stabilization regressions passed **109 tests, 1 warning in 95.19s**; concurrent Confirm passed three additional focused runs. Full suite passed **646 tests, 0 failed, 22 warnings in 308.73s**; runtime OpenAPI remains **58 paths / 70 operations**; `compileall` and `git diff --check` passed. Rollback is limited to the local Confirm guard, focused tests, and this documentation; no data rollback. **NO NEW ADR NEEDED.**

## R7 Exit Review — Stabilization Closure — COMPLETE (2026-09-12)

- **Decision:** **R7 EXIT REVIEW COMPLETE. R7 COMPLETE.** R7 met its stabilization goal without treating code uniformity as a correctness requirement: Entry Review revalidated post-R1–R6 integration/concurrency/fallback/compatibility boundaries, identified the one reproducible HIGH `PO_CONFIRM_OPTIMISTIC_CONCURRENCY_GAP`, and R7.1 closed it with a local database version/state guard.
- **Final concurrency and transaction evidence:** concurrent Confirm from `draft` version `V` now has exactly one success and one `VERSION_CONFLICT`, final `ordered` version `V+1`, one budget reservation, and one `purchase_order_confirm` audit. The guard remains in the caller-owned confirmation transaction; injected budget and audit failures roll it back. R6 Import processing claim/short start-audit/terminal boundaries, PO Receive winner-only guard, and DecisionRun idempotency plus separate assistant fault-isolation commits remain green.
- **Final stability conclusion:** no reproduced HIGH/BLOCKER current-version defect, public contract drift, store-scope breach, raw error-path leak, authority drift, fallback failure, compatibility regression, hidden commit, or material new SQLite long-write hotspot was found. Import crash or double failure in `_mark_failed()` remains `DEFERRED_OPERATIONAL_RECOVERY_RISK`; draft PO PATCH concurrency remains `OBSERVATION_ONLY` without a reproduced violation. Mixed ORM/repository use, active legacy compatibility, core/shadow overlap, and permissive current JSON reads remain non-blocking debt. Direct ORM with explicit owner is acceptable; Repository-everywhere and a global Unit of Work remain unjustified.
- **Verification:** final R7 targeted gate passed **195 tests, 5 warnings in 289.69s**; full suite passed **646 tests, 0 failed, 22 warnings in 552.42s**. Runtime OpenAPI remains **58 paths / 70 operations**; `compileall` and `git diff --check` passed. The 22 warnings remain one Starlette/httpx TestClient dependency deprecation plus 21 Python SQLite datetime-adapter deprecations in tests/migrations. Exit Review production diff: **NONE**. **NO R7.2 IS JUSTIFIED. NO NEW ADR NEEDED.**

## Next phase

## Post-R7 Decision Package no-feasible conformance fix — COMPLETE (2026-09-12)

- **CURRENT FACT:** A valid core Decision Run with no feasible candidate has `recommended_strategy=None` and status `completed_with_no_feasible_recommendation`. The package producer previously selected `{}` in that branch and persisted `critic={}`, although the current typed `DecisionPackage` contract requires `critic.status`.
- **ACCEPTED DECISION:** The package boundary now retains the selected candidate critic unchanged when a recommendation exists; otherwise it emits a structurally valid failed aggregate critic. Findings are deduplicated by code with existing evidence retained per strategy, and warnings are a deterministic union. No candidate is selected or presented as recommended.
- **TECHNICAL DEBT:** Nested Decision Package diagnostics remain intentionally broad JSON objects; this narrow correction does not redesign those schemas.
- **HISTORICAL INFORMATION:** No historical Decision Run was rewritten or migrated. This is current producer conformance under ADR-007; ADR-013 continues to govern disposable SQLite state.
- **PROPOSAL:** None. No ADR, business-feasibility, strategy-selection, warning/hard-violation, persistence, or LLM authority change is needed.
- **Verification:** real-core zero-budget API regression plus Decision Intelligence tests: **17 passed, 1 warning**; full suite: **647 passed, 22 warnings in 245.28s**; generated OpenAPI remains **58 paths / 70 operations** with both Decision Run success responses referencing `DecisionPackage`; `compileall` and `git diff --check` passed.

**R1 COMPLETE. R2 COMPLETE. R3 COMPLETE. R4 COMPLETE. R5 COMPLETE. R5 Entry Review and R5.1–R5.7 COMPLETE. R6 COMPLETE. R6 Entry Review and R6.1–R6.7 plus R6 Exit Review COMPLETE. R7 COMPLETE. R7 Entry Review, R7.1, and R7 Exit Review COMPLETE. Post-R7 Decision Package no-feasible conformance fix COMPLETE. NEXT PHASE NOT YET DEFINED. R4 decomposition direction ACCEPTED. R4 ADR blocker RESOLVED.**

## Explicitly NOT doing yet

- No big-bang core merge.
- No `DecisionPlanningService` rewrite.
- No database redesign.
- No public API redesign.
- No deterministic-first Overall Summary migration.
- No removal of all Qwen code.
- No folder-wide restructure.
- No historical Decision Run migration/backfill in R1.2; ADR-013 permits only directly justified disposable-persistence cleanup.

## DOC-CLEANUP-0

- Canonical documentation set established and retained at the top level of `docs/`.
- Historical documentation archived under `docs/archive/`; redundant superseded documents deleted after reference review.
- R1.1 through R1.7 are complete; R1 has passed its phase-close review. R2 entry review and R2.1–R2.3 are complete; R2 has passed its current approved phase-close review and R3 has not started.
